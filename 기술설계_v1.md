# 기술 설계서 v1 — 웹소설 AI 집필·관리 대시보드 (jippeel)

> **단계**: 표준 파이프라인 4단계 (기술 설계) / 담당: Backend팀 (Backend Architect 주도)
> **작성일**: 2026-08-26
> **상위 문서**: `대시보드_MVP_사양.md` v0.4 (G3 통과), `요구사항_정의서.md` (R-001~R-054, G2 통과), `HANDOFF.md`
> **검증 방식**: 리스크 C9(가정 A3) 실측 검증을 최우선 수행 → 파일 근거·실행 실측만 기재 (추측 없음). 스택은 HANDOFF 확정안 준수.
> **참고**: 본 설계는 이미 구현된 코드(`backend/`, Sprint 1~3, 105 테스트)와의 정합 대조를 포함한다. 불일치 1건(T-002 volume nullable)을 §4.2에 변경 항목으로 명시한다.

---

## 0. 가정 A3 / 리스크 C9 검증 결과 (최우선 과제) — ✅ 성립

### 0.1 결론

**A3는 "조건부 가능"으로 판정한다.** im-not-ai(Humanize KR v2.3.2)는 앱 프로세스에서 호출 가능하다.
정량 지표(shim)·게이트는 **모듈 임포트와 subprocess CLI 양쪽 모두 실측 성공**했다. 진단·윤문 단계만
예외적으로 외부 LLM 에이전트 명령 주입이 필요한데, 이는 스킬 자체의 설계상 필연이므로 A3 가정
("스크립트/모듈 호출 가능")은 성립한다.

### 0.2 파일 근거

| # | 근거 | 파일/경로 |
|---|------|-----------|
| 1 | 스킬 본체는 심링크: `/home/wj941/.agents/skills/humanize-korean` → `~/.agents/im-not-ai/skills/humanize-korean` | `ls -la ~/.agents/skills/` 실측 |
| 2 | 정량 엔진 `metrics_v2.py`는 **표준 라이브러리 전용**(json/re/math/collections/os/sys/argparse/statistics)으로 명시 — 외부 의존 0 | `references/metrics_v2.py` 모듈 docstring "Hard rule: standard library ONLY" |
| 3 | 진입점 함수 존재: `compute_all_v2(...)` (L718), `change_rate(before, after)` (L664, 철칙 #4 SSOT), CLI `_main` (L785) | `grep -n "def "` 실측 |
| 4 | 파이프라인 스크립트 3종 존재: `scripts/prepare_monolith_input.py`, `verify_gates.py`, `reassemble_chunks.py` | `ls ~/.agents/im-not-ai/scripts/` 실측 |
| 5 | SKILL.md 경로 규칙: 스크립트는 설치 루트(`.claude-plugin` 상위), 데이터(`_workspace/`)는 cwd 기준 — 절대경로 호출 필수 | `SKILL.md` §"스크립트 경로 규칙" |

### 0.3 실행 실측 (2026-08-26)

1. **프로세스 내 모듈 호출 — 성공**
   ```python
   import sys; sys.path.insert(0, "~/.agents/skills/humanize-korean/references")
   import metrics_v2 as m2
   r = m2.compute_all_v2("테스트 문장이다. ...", genre="essay")
   # → dict 반환: risk_band="low", metrics 8축, change_rate() 호출 성공
   ```
2. **subprocess CLI — 성공**
   ```
   python3 ~/.agents/im-not-ai/scripts/prepare_monolith_input.py \
     --text "<원문>" --run-dir run1 --genre essay
   # → run1/00_metrics.json 생성: route_hint="light", risk_band="low",
   #   keys=[char_count, evidence, genre, metrics, risk_band, risk_score,
   #         route_hint, route_reason, route_signals, v2_*, version, z_scores]
   python3 ~/.agents/im-not-ai/scripts/verify_gates.py --before 01_input.txt --after final.md --json
   # → CLI 정상 (--help 실측)
   ```
3. **진단/윤문 단계 — 모듈 함수 아님 (설계상 확인)**: SKILL.md의 standard/heavy 경로에서 진단과 윤문은
   LLM 에이전트 콜(예: `claude -p ...`)이다. Python 함수로 존재하지 않으므로 **외부 명령 주입(subprocess)**이
   유일한 연동 방식이다.

### 0.4 설계 반영 (M5 아키텍처 확정)

- **연동 방식 확정**: `backend/app/services/humanize.py` = subprocess 래퍼.
  - shim/게이트: `python3 {IM_NOT_AI_ROOT}/scripts/*.py` 직접 실행 (실측 통과).
    루트는 환경변수 `IM_NOT_AI_ROOT`(기본 `~/.agents/im-not-ai`)로 설정.
  - 진단/윤문: 환경변수 `IM_NOT_AI_DIAGNOSE_CMD` / `IM_NOT_AI_REFINE_CMD` 템플릿 주입
    (placeholder `{run_dir} {input} {input_combined} {diagnosis} {output}`). 미설정 시
    `HumanizeNotConfigured` → HTTP 503 (한국어 안내). 이는 A3 실패 시 M5 전체가 막히지 않도록 하는
    graceful degrade이기도 하다 — 로컬 윤문(정량 지표·게이트)은 명령 미설정 상태에서도 동작 가능.
  - 프로세스 내 모듈 호출(metrics_v2 임포트)은 백엔드 venv 오염 위험이 없어(표준라이브러리 전용) 허용되나,
    **단일 경로 원칙(NFR-502)**에 따라 MVP는 subprocess로 통일한다. 모듈 직접 임포트는 성능 문제
    (회차 5천~1만 자 × 빈번한 route_hint 재판정)가 실측될 때의 최적화 후보로만 보류.
- **변경률 게이트 이중 방어**: im-not-ai `verify_gates.py` 판정 + 백엔드 자체 강제
  (`WARN_RATIO=0.30`, `BLOCK_RATIO=0.50` — `humanize.py` 상수, FR-505). block 시 result_text 저장은 하되
  accept를 409로 거부한다.

---

## 1. 스택 확정 (HANDOFF 확정안 준수)

| 레이어 | 확정 | 근거 |
|--------|------|------|
| 백엔드 | **Python 3.12+ FastAPI + SQLAlchemy 2.x + SQLite(WAL) + Alembic** | HANDOFF 확정안 그대로. 단일 사용자 로컬 앱(C2), SSE 스트리밍(FR-405), 파일 1개 백업(NFR-203). 이미 Sprint 1~3 구현·105 테스트 통과 |
| AI 통신 | **openai SDK(base_url 오버라이드) 단일 경로, httpx 폴백**, Ollama 네이티브 불사(C3) | HANDOFF 확정안 "모델 무관 OpenAI 호환". LiteLLM 배제(부록05 §④-3, NFR-502) |
| 프론트 | **Vite + React(TS) + shadcn/ui + Tailwind + Zustand + TanStack Query + CodeMirror 6** 유지 | 사양 v0.4 §2.1 사용자 최종 결정(2026-08-25) + Sprint 4 완료·빌드 통과. |

### 1.1 프론트 경량화 방식 권고 (근거 표기)

"바닐라 JS 무빌드" 대안도 검토했으나, **유지(React 경량 빌드)를 권고**한다.

- **권고**: Vite+React 유지 + 다음 3개 경량화 장치.
  1. 코드 스플리팅 페이지 단위(S1~S7 lazy import) — 초기 번호 축소.
  2. CodeMirror 6·markdown-it 등 중형 의존은 동적 import(에디터 탭 진입 시 로드).
  3. `vite build` 산출물을 FastAPI `StaticFiles`로 서빙 → 운영 프로세스 1개(uvicorn 단독),
     배포물은 정적 폴더 복사뿐. Node는 개발 시에만 필요.
- **근거**: ① 사양 v0.3/v0.4에서 React가 사용자 최종 결정이며 프론트가 이미 완성(Sprint 4a·4b, E2E 6/6)되어
  교체 비용 > 편익. ② CodeMirror 6·jsdiff·TanStack Query 등 핵심 라이브러리는 프레임워크 무관이나
  상태관리·SSE 소비(aiStream.ts)·diff 리포트 UI가 이미 React 결합. ③ 바닐라 JS 전환은 번들러 제거 대신
  수작업 모듈 관리 비용을 만들며, 1인 비전공자 운영(C6)에는 "빌드 커맨드 1개(Jippeel실행.bat 내장)"가
  오히려 단순하다. ④ 소규모 운영·비용 관점의 실부하는 빌드가 아니라 **실행 프로세스 수**이므로,
  정적 서빙 통합으로 uvicorn 1프로세스에 해결한다.

---

## 2. 시스템 구성도 (텍스트)

```
┌─────────────────────────── 사용자 PC (Windows, C6) ───────────────────────────┐
│                                                                              │
│  [Browser]  http://localhost:8170                                            │
│     │  S1 홈 / S2 에디터(CodeMirror6) / S3 캐릭터 / S4 로어북                  │
│     │  S5 AI 패널(fetch 스트림 SSE) / S6 윤문 리포트(jsdiff) / S7 설정          │
│     ▼                                                                        │
│  [FastAPI :8000]  /api/v1/*                                                  │
│     │                                                                        │
│     ├─ routers/projects.py ──┐                                               │
│     ├─ routers/characters.py ├─ SQLAlchemy 2.x Session ──► jippeel.db        │
│     ├─ routers/lorebook.py   │   (WAL/NORMAL/busy_timeout/FK 프라그마)   (SQLite+F TS5)│
│     ├─ routers/ai_panel.py   │                                               │
│     ├─ routers/refine.py ────┤                                               │
│     │                        │                                               │
│     ├─ services/llm.py ──────┼─ openai SDK(base_url 오버라이드) ──► LLM 엔드포인트 │
│     │  (SSE 스트리밍 프록시) │      LM Studio/KoboldCpp/클라우드 (C3, Ollama 네이티브 불사)│
│     │                        │                                               │
│     ├─ services/crypto.py ───┼─ DPAPI(keyring) 우선 → Fernet 로컬키 폴백       │
│     │                        │                                               │
│     └─ services/humanize.py ─┴─ subprocess ──► ~/.agents/im-not-ai/scripts/  │
│                            │      prepare_monolith_input.py (route_hint)     │
│                            │      verify_gates.py (변경률·구조 게이트)         │
│                            └─ subprocess ──► IM_NOT_AI_DIAGNOSE_CMD/REFINE_CMD │
│                                              (외부 LLM 에이전트 명령 — §0.4)   │
└──────────────────────────────────────────────────────────────────────────────┘
```

- 인증 없음(C2 로컬 단일 사용자). CORS는 개발 시 Vite dev 프록시(/api), 운영은 동일 origin(StaticFiles 서빙).
- Alembic: `alembic.ini` script_location 고정, `render_as_batch`(SQLite ALTER 대응).

---

## 3. API 설계 (A-### 부여)

공통 prefix `/api/v1`. 응답 형식은 Pydantic v2 스키마(`schemas.py`). 각 A의 대응 F-xxx는 사양 v0.4 §9 기준.
구현 상태 표기: ✅=이미 구현(테스트 통과), 🔧=본 설계에서 신규/변경 요구.

### 3.1 프로젝트·회차 (M1)

| A-ID | 메서드/경로 | F-xxx | 비고 |
|------|-------------|-------|------|
| A-001 | POST `/projects/bootstrap` | F-001 | 작품 부트스트랩(제목·목차·캐릭터·로어북 LLM 3콜 생성, 단일 트랜잭션) ✅ |
| A-002 | GET `/projects` | F-001 | 목록 ✅ |
| A-003 | POST `/projects` | F-001 | 생성 (201) ✅ |
| A-004 | GET `/projects/{pid}` | F-001 | ✅ |
| A-005 | PATCH `/projects/{pid}` | F-001 | ✅ |
| A-006 | DELETE `/projects/{pid}` | F-001 | cascade 삭제 ✅ |
| A-007 | GET `/projects/{pid}/chapters?volume=` | F-002 | 회차 트리(volume 필터 지원) ✅ |
| A-008 | POST `/projects/{pid}/chapters` | F-002 | 생성 ✅ |
| A-009 | GET `/chapters/{cid}` | F-003, F-007 | 본문 포함 상세 ✅ |
| A-010 | PATCH `/chapters/{cid}` | F-005, F-006, F-007 | 부분 갱신(제목/상태/memo). 자동저장 디바운스 대상 ✅ |
| A-011 | PUT `/chapters/{cid}/content` | F-006 | 본문 전용 저장 + word_count_cache 갱신 ✅ |
| A-012 | DELETE `/chapters/{cid}` | F-002 | ✅ |
| A-013 | PATCH `/projects/{pid}/chapters/reorder` | F-002 | bulk 순서/권 변경. 소속 불일치·중복 id → 422, 부분 적용 없음 ✅ |

### 3.2 캐릭터 (M2)

| A-ID | 메서드/경로 | F-xxx | 비고 |
|------|-------------|-------|------|
| A-014 | GET `/projects/{pid}/characters` | F-009, F-010 | 카드 목록(+관계 포함) ✅ |
| A-015 | POST `/projects/{pid}/characters` | F-009 | ✅ |
| A-016 | GET `/characters/{chid}` | F-009 | ✅ |
| A-017 | PATCH `/characters/{chid}` | F-009 | 7필드 부분 갱신 ✅ |
| A-018 | PATCH `/characters/{chid}/card_json` | F-011 | RFC 7386 유사 JSON 병합 ✅ |
| A-019 | DELETE `/characters/{chid}` | F-009 | 관계 cascade ✅ |
| A-020 | POST `/projects/{pid}/characters/relations` | F-010 | 양측 동일 프로젝트 검증(422) ✅ |
| A-021 | GET `/characters/{chid}/relations` | F-010 | 양방향 조회 ✅ |
| A-022 | DELETE `/relations/{rid}` | F-010 | ✅ |

### 3.3 로어북 (M3)

| A-ID | 메서드/경로 | F-xxx | 비고 |
|------|-------------|-------|------|
| A-023 | GET `/projects/{pid}/lore?category=` | F-012, F-013 | 카테고리 필터 ✅ |
| A-024 | POST `/projects/{pid}/lore` | F-012 | ✅ |
| A-025 | GET `/projects/{pid}/lore/search?q=` | F-015, F-016 | FTS5 prefix 질의, 미지원 시 LIKE 폴백(`services/fts.py`) ✅ |
| A-026 | GET/PATCH/DELETE `/lore/{lid}` | F-012 | ✅ |
| A-027 | PUT `/lore/{lid}/keywords` | F-014 | keywords[] 전체 치환 + FTS 동기화 ✅ |

### 3.4 AI 패널 (M4)

| A-ID | 메서드/경로 | F-xxx | 비고 |
|------|-------------|-------|------|
| A-028 | GET `/ai/endpoints` | F-017, F-018, F-023 | api_key 마스킹 응답 ✅ |
| A-029 | POST `/ai/endpoints` | F-017, F-018 | 평문 key 수신 즉시 암호화 저장 ✅ |
| A-030 | PATCH/DELETE `/ai/endpoints/{eid}` | F-017, F-023 | 기본값 플래그 포함 ✅ |
| A-031 | GET `/ai/endpoints/{eid}/models` | F-017 | `{base_url}/models` 프록시 ✅ |
| A-032 | GET/POST `/ai/presets`, PATCH/DELETE `/ai/presets/{preset_id}` | F-019 | 초기 5종 시드(Q3: 장면생성·대사보강·요약·브레인스토밍·설정질의) ✅ |
| A-033 | POST `/ai/generate` (SSE) | F-020, F-021, F-024 | context 선택(chapter/character_ids/lore_ids) 조립 → 스트리밍 프록시. 오류 3종 한국어 매핑 ✅ |

### 3.5 윤문 (M5)

| A-ID | 메서드/경로 | F-xxx | 비고 |
|------|-------------|-------|------|
| A-034 | POST `/refine` | F-025, F-026, F-027, F-029, F-031 | body `{chapter_id, force_route?, genre?}` → `{run_id, route_hint, spans[A~J], original, refined, changed_ratio, gate}`. span 오프셋은 원문 기준 문자 위치(§5 응답 규격 준수) ✅ |
| A-035 | GET `/refine/runs/{run_id}` | F-031 | 리포트 재조회 ✅ |
| A-036 | POST `/refine/runs/{run_id}/accept` | F-028 | chapter.content_md 교체 + accepted=true. gate=block이면 409 ✅ |
| A-037 | POST `/refine/runs/{run_id}/reject` | F-028 | 기록만 남김(원문 보존) ✅ |

### 3.6 공통/기반 (F-030~F-036)

| A-ID | 대상 | F-xxx | 비고 |
|------|------|-------|------|
| A-038 | GET `/projects/{pid}/plus-status` (신규) | F-033 | 노벨피아 PLUS 충족 현황: 연작 수≥15·완료 회차 공백제외≥3000자 서버 계산. 🔧 신규 구현 필요(계산 로직은 wordcount 서비스 재사용) |
| A-039 | 백업/복원 | F-034 | **엔드포인트 없음(파일 단위 수동)** — Q5 결정안: MVP는 수동 버튼만. 구현 방식: 앱 종료 상태에서 jippeel.db(-wal/-shm 포함) 파일 복사 안내 + S7에서 복사 대상 경로 표시. REST 엔드포인트는 만들지 않음(WAL 핫카피 정합 리스크 회피) |
| A-040 | 정적 서빙 | F-035 | `GET /` → `frontend/dist` StaticFiles. Jippeel신행.bat → uvicorn 단일 프로세스 기동 🔧 (dev.sh는 분기 유지) |

- F-030(용도 고지), F-032(규정 요약·확인 시점), F-036(라이선스 고지): 백엔드 API 없음. 프론트 정적 문구/About로 충족 — traceability frontend 필드에서 관리.
- F-008(.txt/.md/jszip 내보내기): 프론트 lib/export.ts에서 클라이언트 생성(서버 부하 0) — API 없음.

---

## 4. 데이터 모델 (T-### 부여)

DB: SQLite (WAL). PK는 모두 autoincrement integer. 모든 테이블에 created_at/updated_at(TimestampMixin, UTC).
§11 Q1~Q5 결정안을 굵게 표기했다.

### T-001 Project
| 컬럼 | 타입 | 제약 |
|------|------|------|
| id | INTEGER | PK |
| title | VARCHAR(255) | NOT NULL |
| genre | VARCHAR(100) | nullable |
| synopsis | TEXT | nullable |
| platform_note | TEXT | nullable |
| memo | TEXT | nullable (부트스트랩 메타) |

### T-002 Chapter — **🔧 변경 필요: volume nullable (Q1 결정안)**
| 컬럼 | 타입 | 제약 |
|------|------|------|
| id | INTEGER | PK |
| project_id | INTEGER | FK→T-001.id, NOT NULL, index |
| **volume** | INTEGER | **NULL 허용 (Q1: 권 없는 평면 회차 지원)** — 현재 코드는 NOT NULL default=1 → Alembic batch ALTER 필요 |
| sort_order | FLOAT | 권 내 정렬(실수형으로 끼워넣기 허용) |
| title | VARCHAR(255) | default "" |
| content_md | TEXT | 마크다운 통짜(장면 분해는 백로그 P2) |
| status | VARCHAR(20) | 초고\|수정중\|완료 (default 초고, F-005) |
| word_count_cache | INTEGER | 공백 제외 카운트 캐시(F-004, C8 검증 대상) |
| memo | TEXT | nullable (F-007) |

### T-003 Character
| 컬럼 | 타입 | 제약 |
|------|------|------|
| id | INTEGER | PK |
| project_id | INTEGER | FK, NOT NULL, index |
| name | VARCHAR(255) | NOT NULL |
| aliases | JSON | list |
| role | VARCHAR(50) | 주연\|조연\|... |
| appearance / personality / speech_style / background | TEXT | nullable (7필드 중 4) |
| card_json | JSON | ST V2/V3 매핑 여지(F-011) |

### T-004 Relationship — **Q2: 텍스트 라벨 확정**
| 컬럼 | 타입 | 제약 |
|------|------|------|
| id | INTEGER | PK |
| from_character_id / to_character_id | INTEGER | FK→T-003, NOT NULL, index |
| label | VARCHAR(255) | 예: "주군-가신" (Q2: 그래프 표현은 백로그) |
| note | TEXT | nullable |

### T-005 LoreEntry
| id(PK) | project_id FK idx | category VARCHAR(50) 기타 | title VARCHAR(255) NN | content TEXT | keywords JSON |
커버리지: 용어\|장소\|세력\|기타 (F-013). FTS5 커버 테이블 `lore_fts`(title/content/keywords, unicode61) — CRUD에서 동기화, 미지원 빌드 시 LIKE 폴백.

### T-006 AiEndpoint
| id(PK) | name VARCHAR(255) NN | base_url VARCHAR(512) | api_key_encrypted TEXT nullable(암호문만) | default_model VARCHAR(255) | temperature FLOAT 0.7 | is_default BOOL |

### T-007 PromptPreset
| id(PK) | name VARCHAR(255) NN | template_text TEXT NN | context_flags JSON ["chapter","characters","lore"] |
초기 시드 5종(Q3).

### T-008 RefineRun
| id(PK) | chapter_id FK idx | route_hint VARCHAR(20) light\|standard\|heavy | changed_ratio FLOAT | report_json JSON(spans·gate 판정) | result_text TEXT | accepted BOOL |

### ERD 요약
```
Project 1─N Chapter        Project 1─N Character       Character N─N Character (via T-004)
Project 1─N LoreEntry      Chapter 1─N RefineRun
AiEndpoint·PromptPreset: 전역(프로젝트 독립)
```

### 4.2 기존 코드 대비 변경 항목 (G4 판정 결과)
1. **T-002.volume nullable 전환** — 현재 `Mapped[int] default=1`(non-null). Q1 결정안 위반.
   조치: `mapped_column(Integer, nullable=True, default=None)` + Alembic revision(batch mode) + schemas/projects.py의 volume 필드 Optional화 + reorder 로직의 None 정렬 처리(None last).
2. A-038 plus-status 엔드포인트 신규(F-033 — 현재 미구현).
3. A-040 정적 서빙 운영 모드(현재 dev 분리) — bat 스크립트 갱신.
그 외 API·모델·서비스는 구현 코드와 일치(Sprint 1~3, 105 테스트).

---

## 5. Transaction 설계

원칙: 요청 1건 = 세션 1개 = 트랜잭션 1개. get_db() 의존성으로 요청 종료 시 commit/rollback 보장.

| TX-ID | 대상 A | 트랜잭션 범위 | 실패 처리 |
|-------|--------|--------------|-----------|
| TX-01 | A-003~A-013 단건 CRUD | 단일 INSERT/UPDATE/DELETE + commit | 예외 시 rollback → 4xx/500, 부분 상태 없음 |
| TX-02 | A-013 reorder | 전체 대상 행 로드 → 검증(소속·중복) → 일괄 갱신 → commit. **검증 실패 시 어떤 row도 갱신 금지**(원자성, HANDOFF Sprint 2 명세) | 422, 롤백 |
| TX-03 | A-001 bootstrap | LLM 3콜(트랜잭션 외부) → 파싱 성공 후 Project+Chapter+Character+Relationship+LoreEntry **단일 commit**. JSON 파싱 실패 시 재시도 → 규칙 기반 폴백 → 그래도 실패 시 502, DB 기록 0 | 502, 롤백 |
| TX-04 | A-029/A-030 endpoint 저장 | 암호화(crypto) → INSERT/UPDATE → commit. 암호화 실패 시 DB 기록 없음 | 500 |
| TX-05 | A-034 refine | RefineRun INSERT(실행 전 pending 기록) → subprocess 파이프라인(**트랜잭션 외부, 최대 900s**) → 결과로 UPDATE. 프로세스 크래시 시에도 run 기록残存(재조회 가능) | HumanizeError → 502 / NotConfigured → 503 |
| TX-06 | A-036 accept | gate 재확인(block→409) → chapter.content_md 교체 + RefineRun.accepted=true → **단일 commit**(본문 교체와 이력이 함께 원자 반영) | 409 |
| TX-07 | FTS 동기화 | lore CRUD와 같은 세션/트랜잭션에서 fts 테이블 DML 수행 — 본문과 색인 불일치 없음 | 롤백 시 색인도 함께 롤백 |

SQLite 프라그마(전 세션 공통): `journal_mode=WAL, synchronous=NORMAL, busy_timeout=5000, foreign_keys=ON`.
WAL로 자동저장(A-011 빈발 write)과 조회 병행 + kill -9 후 마지막 커밋 보존(R-044, F-006).

## 6. Security 설계

**목표: api_key 평문 0건 (R-042, Success Metrics "저장 파일 덤프 0건")**

1. **저장 계층 (`services/crypto.py`)**
   - 1순위 **Windows DPAPI(keyring)**: Fernet 마스터 키를 OS 자격증명 저장소에 저장(KEYRING_SERVICE/ENTRY), 본문은 Fernet 암호화 → DB에는 암호문만.
   - 폴백 **Fernet 로컬 키 파일**: keyring 백엔드 부재(dbus 없는 Linux 등)·쓰기 실패 시. 키 파일 경로·위협 모델(동일 PC 로컬 공격자 방어 한계)을 S7 고급 탭 + README에 문서화할 의무(사양 §8-2).
   - `PlaceholderCipher`(테스트 전용)는 프로덕션 설정에서 금지.
2. **전송/표시 계층**: 응답 스키마는 `api_key_masked`만 노출(앞 3자 + ***). 평문 key를 되돌려주는 GET 없음. 프론트 S7 마스킹만 표시([표시] 버튼 없음 — 프로토타입 C-1 결정 준수).
3. **LLM 호출 시**: decrypt는 `services/llm.make_client()` 내부에서만 발생, 로그·예외 메시지에 key 미포함.
4. **subprocess 위생(humanize)**: 외부 명령 템플릿의 placeholder는 shlex.quote 후 치환. run_dir는 tempfile 기반 세션별 격리. shell=True는 템플릿 문법 허용을 위해 한정 사용하며, 입력 원문은 파일 경유(argv 직접 전달 금지).
5. **미인증 로컬 바인딩**: uvicorn은 127.0.0.1 바인딩 기본(외부 노출 방지). CORS는 dev 프록시에 한정.
6. **XSS**: 미리보기 markdown-it + DOMPurify(프론트 담당, F-003) — 백엔드는 HTML을 생성하지 않음.

## 7. Error Handling 설계

원칙: 모든 오류는 (HTTP 상태, 한국어 detail, 원인 구분) 3요소. R-027/F-024의 3종 안내를 충족.

| 상황 | 상태 | detail(한국어) 예 | 매핑 |
|------|------|-------------------|------|
| 리소스 없음 | 404 | "프로젝트를 찾을 수 없습니다." | 공통 |
| 검증 실패(reorder 중복·소속 불일치, 관계 cross-project) | 422 | "회차 목록이 잘못되었습니다. (중복/소속 불일치)" | Pydantic ValidationError 포함 |
| LLM 연결 실패/타임아웃(openai.APIError 계열) | 502 | "AI 엔드포인트에 연결할 수 없습니다. base_url과 네트워크를 확인하세요." | F-024-1 |
| LLM 인증 실패(401/403 전파) | 502 | "API 키 인증에 실패했습니다. 키를 다시 확인하세요." | F-024-2 |
| LLM 타임아웃 | 504 | "응답이 지연되어 중단했습니다. max_tokens를 줄이거나 다시 시도하세요." | F-024-3 |
| im-not_ai 미설정(DIAGNOSE/REFINE CMD 없음) | 503 | "윤문 엔진이 설정되지 않았습니다. S7 → 윤문 탭에서 명령을 등록하세요." | HumanizeNotConfigured |
| im-not_ai 실행 실패/타임아웃(900s) | 502 | "윤문 파이프라인 실행 실패: {stderr 요약}" | HumanizeError |
| 변경률 게이트 block | 200(gate=block) + accept 시 409 | "변경률이 50%를 초과했습니다. 수락할 수 없습니다." | F-029 |
| 부트스트랩 LLM 출력 파싱 최종 실패 | 502 | "작품 자동 생성에 실패했습니다. 프리미스를 다시 입력해 보세요." | TX-03 |

- SSE 스트리밍(A-033) 중 오류는 이벤트 `event: error` + data로 전달해 프론트 토스트로 표시(연결 끊김과 구분).
- 전역 핸들러: unhandled Exception → 500 + 로그(detail에 내부 경로·key 노출 금지).

## 8. im-not-ai 연동 방식 (요약 — 상세는 §0)

```
POST /refine
  1) RefineRun(pending) insert            [TX-05, 트랜잭션 1]
  2) tempfile run_dir 생성, 01_input.txt 기록
  3) subprocess: prepare_monolith_input.py --run-dir --genre   ← 실측 성공(§0.3)
  4) route_hint 결정: force_route > metrics.route_hint > standard (F-026, degrade 시 standard)
  5) standard/heavy → IM_NOT_AI_DIAGNOSE_CMD 실행 → --diagnosis로 shim 재실행
  6) IM_NOT_AI_REFINE_CMD 실행 → final.md
  7) subprocess: verify_gates.py --before --after --json  (+백엔드 자체 WARN 30/BLOCK 50 강제)
  8) spans 파싱(taxonomy A~J, 문자 오프셋) → RefineRun UPDATE → 응답
수락(A-036): block 아니면 chapter.content_md 교체+accepted (TX-06)
```
외부 AI 차단 상태(R-052)에서도 3)/4)/7)과 게이트 판정은 로컬로 동작한다(진단·윤문 콜만 실패 → 503 안내).

## 9. 프로젝트 디렉터리 구조 (현행 확정 + 변경점)

```
jippeel/
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI 엔트리 + StaticFiles(A-040 🔧)
│   │   ├── database.py          # 프라그마 표준 적용 세션팩토리
│   │   ├── models.py            # T-001~T-008 (🔧 T-002 volume nullable)
│   │   ├── schemas.py           # Pydantic v2 (volume Optional화 🔧)
│   │   ├── routers/             # projects / characters / lorebook / ai_panel / refine
│   │   └── services/            # llm / crypto / humanize / fts / wordcount / bootstrap
│   ├── alembic/                 # 🔧 volume nullable revision 추가
│   ├── tests/                   # 15파일, pytest (변경분 테스트 추가)
│   └── requirements.txt
├── frontend/                    # Vite+React(TS) — 현행 유지(§1.1)
│   ├── e2e/app-flow.spec.ts     # Playwright 6 시나리오
│   └── src/                     # pages/components/stores/lib
├── scripts/dev.sh               # 개발 기동(be+fe 분리)
├── Jippeel실행.bat               # 운영 기동(🔧 uvicorn 단일 프로세스 + dist 서빙으로 갱신)
├── gates.json / traceability.json
└── 기술설계_v1.md               # 본 문서
```

## 10. 열린 질문 (parent 보고용)

1. **Q-A3 후속**: 진단/윤문 명령(IM_NOT_AI_DIAGNOSE_CMD/REFINE_CMD)의 기본값을 무엇으로 할지 — 현재는 미설정 시 503. `claude -p` 프리셋을 S7에 기본 템플릿으로 심을지 사용자 확인 필요.
2. **Q-C8**: 노벨피아 공백 제외 카운트 검증 샘플 — QA(G7) 전까지 실제 샘플 1건 확보 필요(사용자/리서치 지원 요청).
3. **Q-F033**: PLUS 현황(A-038)의 "연작 15편" 기준 — 프로젝트 내 회차 수로 볼지, 별도 연작 그룹 개념이 필요한지 확정 요청(현 설계는 "프로젝트 단위 회차 수≥15" 임시 해석).
4. **Q-TX05**: refine 900s 타임아웃의 UX — 동기 대기 유지(현행) vs 백그라운드 잡+폴링 전환(MVP는 동기 유지 권고).
5. **Q-KEYRING**: Windows 실기기에서 keyring DPAPI 동작 실측 — G7 QA 항목으로 이관 권고.
