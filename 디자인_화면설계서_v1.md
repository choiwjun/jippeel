# 디자인_화면설계서 v1.3 — 웹소설 AI 집필 대시보드 (React + shadcn/ui)

> **문서 위치**: 디자인팀 4단계 산출물 (AGENTS.md §0, §1)
> **기준 사양**: `대시보드_MVP_사양.md` v0.4 — §3 화면 목록(S1~S7), §6 UI 가이드라인, §9 F-001~F-036, §11 Q1~Q5
> **역참조**: `요구사항_정의서.md` FR-101~602 / NFR-101~503 · `기술설계_v1.md` A-001~A-040 / T-001~T-008 · `결정사항_G4.md` Q1~Q4
> **기술 스택**: React + Vite (TypeScript) · shadcn/ui + Tailwind CSS · Zustand(4 스토어) · TanStack Query · CodeMirror 6 (+lang-markdown) · markdown-it + DOMPurify · jsdiff
> **작성 모델**: MiniMax M3 (AGENTS.md §1 — 프론트엔드 디자인 라우팅)
> **v1.1 갱신일**: 2026-08-25 (QA_기획정합성_리포트.md 반영)
> **v1.3 갱신일**: 2026-08-26 (사양 v0.4 F-xxx·기술설계 A-xxx 정합 + S-### ID 체계 + G5 디자인 게이트)
> **v1 최초 작성일**: 2026-08-25

---

## 변경이력 (CHANGELOG)

| 버전 | 날짜 | 변경 요약 | 출처 |
|------|------|-----------|------|
| v1 | 2026-08-25 | 초안 — S1~S7 와이어프레임, 컴포넌트 트리, Tailwind 토큰, Zustand 스토어 경계, 인터랙션 플로우, 핸드오프 매핑 | 디자인팀 1차 |
| **v1.1** | **2026-08-25** | **QA 리포트(C-1·C-2·M-1·M-2·M-3·M-4 + Minor m-1·m-2·m-3·m-6·m-7·m-8) 반영** — ① S7 api_key [표시] 버튼 삭제(마스킹만 노출, 키 변경만 허용) ② S6/§3.2 50% 초과 시 강제 수락 경로 완전 삭제(윤문 재실행·폐기만 제공) ③ S2 에디터 메뉴에 '내보내기(.txt/.md)' 추가 ④ S5 AI 패널 상단 Alert에 NFR-201 전송 고지 문구 추가 ⑤ S7 규정·현황 탭에 NFR-404 안내 문구 추가 ⑥ §2.6·§4.1 윤문 카테고리 색 매핑을 taxonomy ID(A~J) 기준으로 재매핑(리듬 중복 오타 정정) ⑦ §2.2 S2 빈 상태 FR-301→FR-101/103 정정·FAB FR-303 오참조 제거 ⑧ §2.1 S1 카드 행 FR-101 계열로 정정·상태 칩 데이터 출처 명시 ⑨ §1.2 S 구간에 '모바일 미지원·단순 열람 폴백' 명시 ⑩ §7.2 lineNumbers 기본 OFF + 설정 토글 통일 ⑪ §2.6·§3.2 수락 시 자동저장 일시 OFF 로직 제거(invalidate 단순화) | **QA_기획정합성_리포트.md** (디자인팀 처리) |
| **v1.2** | **2026-08-25** | **QA v2 회귀 정정** — ① R-1: §7.7 윤문 수락 행을 §2.6 방식(자동저장 유지 + accept 완료 후 `invalidateQueries(['chapter', cid])` + `['refine-runs', cid]`)으로 정정(OFF→교체→ON 구 표기 제거) ② R-2: §2.5 스트리밍 행의 `EventSource` 2건과 §7.1 백로그 P1-B의 `EventSource` 1건을 `fetch` 스트림(SSE, `ReadableStream` + `getReader()`) 표기로 교체(총 3건) | **QA_기획정합성_리포트_v2.md** (디자인팀 처리) |
| **v1.3** | **2026-08-26** | **G5 디자인 게이트 — 사양 v0.4(F-xxx)·기술설계 v1(A-xxx) 정합 갱신** — ① 전 화면·컴포넌트에 **S-### 고유 ID 부여**(§0-A 레지스트리, 총 53건) 및 각 화면 제목 표기 ② §0-A 신설: S-ID × F-xxx × A-### 매핑표(화면→기능→API 추적 근거) ③ 결정사항_G4 반영: Q1 윤문 진단·윤문 명령 기본 프리셋 2종 초기값 UI를 S7 윤문 탭(S-702)에 추가(T-007 시드, 사용자 편집 가능, env 미설정 시 503 graceful degrade 안내) / Q3 PLUS 현황 문구 "프로젝트 내 회차 수 ≥15회"로 정합(S-104·S-704) / Q4 refine 900s 동기 대기 유지 + 실행 중 진행 상태 UI 신설(S-606) ④ §6.3 시니어 배려 모드(접근성 보강) 신설 — UI 배율·클릭 타겟 확대·고대비 토글·모션 감소(S-703 연동) ⑤ 기준 문서를 사양 v0.4로 상향, 역참조에 A/T 번호 체계 추가 | `대시보드_MVP_사양.md` v0.4 · `기술설계_v1.md` · `결정사항_G4.md` (디자인팀 G5 처리) |

---

## 0. 핵심 UX 원칙 (사양 §6 → 모든 화면에 강제)

| 원칙 | 출처 | 적용 화면 |
|------|------|-----------|
| **P1. AI 결과는 절대 자동 삽입하지 않는다** — "끼워넣기 / 선택 교체 / 복사" 명시 버튼만 허용 (FR-406) | §6 핵심UX 2 | S2, S3, S4, S5 |
| **P2. 글자 수(공백 제외) 항상 노출** — 노벨피아 PLUS 3,000자 조건 확인용 (FR-104) | §6 핵심UX 1 | S2 |
| **P3. 저장 상태 표시 "저장됨 · 방금 전"** — 자동저장 신뢰감 (FR-106) | §6 핵심UX 4 | S2, S3, S4, S7 |
| **P4. 윤문 diff는 사용자 수락/거절 결정** — 자동 덮어쓰기 금지, 변경률 게이트 30% 경고/50% **수락 차단**(강제 수락 경로 없음, 재실행·폐기만 허용 — FR-503~505·사양 v0.3 §8.3·im-not-ai 4대 철칙) | §6 핵심UX 3 | S6 |
| **P5. 카드형 상세는 그리드 + 드로어** (FR-205) | §6 핵심UX 5 | S3 |
| **P6. AI 패널은 어디서든 호출 가능** — Sheet/Drawer로 전역 표시 (NFR-303) | §6 핵심UX | S2/S3/S4/S5 |
| **P7. 저채도 다크/라이트 + 세리프 본문** — shadcn 기본 뉴트럴 팔레트 금지, 소설 도구 톤 | §6 톤 | 전역 |
| **P8. 금지**: 이모지 아이콘 남발, 과한 애니메이션, 정보 밀도 높은 위젯 총출 | §6 금지 | 전역 |

> **AI 관련 모든 화면(S2/S3/S4/S5/S6) 공통 삽입 가드 문구 (2줄 고정)**:
> 1. **P1 자동삽입 금지(FR-406)**: *"AI 결과는 자동으로 본문에 들어가지 않습니다. '끼워넣기' 또는 '선택 교체'를 눌러야 반영됩니다."* → Sheet/Drawer 상단 `<Alert variant="info" icon={<ShieldCheck />}>`로 노출
> 2. **NFR-201 전송 고지(M-2)**: S5에서는 추가로 *"선택한 회차·카드·로어북 내용은 지정한 LLM 엔드포인트로 전송됩니다"* 두 번째 Alert 고정 노출(아이콘 `<CloudUpload />`)
> 3. **P4 윤문 게이트(C-2)**: S6에서는 변경률 ≥ 50% 시 수락 버튼 disabled + 강제 수락 경로 없음. 선택지는 [윤문 재실행] / [폐기] 두 가지뿐

---

## 0-A. 화면·컴포넌트 S-ID 레지스트리 & 화면 ↔ F-xxx ↔ A-### 매핑표 (v1.3 신설)

> 본 문서의 모든 화면·컴포넌트는 **S-### 고유 ID**를 갖는다. 대응 기능은 사양 v0.4 §9의 **F-xxx**, 화면이 호출하는 백엔드 API는 기술설계 v1 §3의 **A-###**이다.
> traceability.json의 design 필드가 이 표의 S-###을 참조한다. "—"은 해당 계층 호출이 없음(정적 문구·클라이언트 생성)을 뜻한다.

### 0-A-1. 전역 레이아웃·공통 컴포넌트 (S-001~S-009)

| S-ID | 이름 | 위치 | 대응 F-xxx | 참조 API(A-###) |
|------|------|------|-----------|----------------|
| S-001 | 앱 셸(3분할 레이아웃 + 라우팅) | §1.1, §1.3 | F-035 | A-040 |
| S-002 | TopBar(로고·프로젝트 셀렉터·저장 표시) | §1.1 | F-001, F-006 | A-002 |
| S-003 | LeftSidebar | §1.1 | F-002, F-035 | A-007 |
| S-004 | RightPanelSheet(S5/S6 겹쳐 열림 오버레이) | §1.1, §1.3 | F-017, F-021, F-025 | A-033, A-034 |
| S-005 | StatusBar(현재 회차·글자 수·저장 상태·AI 상태) | §1.1 | F-004, F-006, F-021 | A-010, A-011, A-033 |
| S-006 | ChapterTree(회차 트리, 권/화) | §1.3, §2.2 | F-002 | A-007, A-008, A-010, A-012, A-013 |
| S-007 | ProjectFilterList(S1 좌측 필터) | §1.3 | F-001 | A-002 |
| S-008 | SettingsNav(S7 좌측 네비) | §1.3 | F-017, F-018, F-019 | A-028, A-032 |
| S-009 | Toaster(전역 토스트, sonner) | §1.3 | F-024, F-035 | — |

### 0-A-2. 화면별 컴포넌트 (S-100~)

| S-ID | 이름 | 위치 | 대응 F-xxx | 참조 API(A-###) |
|------|------|------|-----------|----------------|
| **S-100** | S1 홈 / 프로젝트 목록(화면) | §2.1 | F-001, F-032, F-033 | A-002, A-003, A-038 |
| S-101 | 프로젝트 카드 그리드 | §2.1 | F-001 | A-002, A-004, A-005, A-006 |
| S-102 | 프로젝트 생성 Dialog(부트스트랩) | §2.1 | F-001 | A-001, A-003 |
| S-103 | 규정 요약 카드(S1 하단) | §2.1 | F-032 | — |
| S-104 | 노벨피아 PLUS 충족 현황 위젯(S1) | §2.1 | F-033 | A-038 |
| S-105 | 최근 작업 회차 리스트(이어쓰기) | §2.1 | F-001 | A-002, A-007 |
| S-106 | 빈 상태 / 로딩 스켈레톤(S1) | §2.1 | F-001, F-035 | — |
| **S-200** | S2 회차 에디터(화면) | §2.2 | F-002, F-003, F-004, F-005, F-006, F-007, F-008 | A-007, A-008, A-009, A-010, A-011, A-012, A-013 |
| S-201 | CodeMirror 6 에디터 본체 | §2.2 | F-003, F-006 | A-009, A-011 |
| S-202 | 미리보기 탭(markdown-it+DOMPurify) | §2.2 | F-003 | A-009 |
| S-203 | 글자 수 푸터(공백 제외 강조) | §2.2 | F-004 | A-009, A-011 |
| S-204 | 저장 상태 인디케이터("저장됨 · 방금 전") | §2.2 | F-006 | A-010, A-011 |
| S-205 | 에디터 메뉴(회차/프로젝트 내보내기 .txt/.md/zip) | §2.2 | F-008 | — (클라이언트 jszip 생성) |
| S-206 | 회차 메모 Popover(chapter.memo) | §2.2 | F-007 | A-009, A-010 |
| S-207 | 빠른 액션 FAB(AI 패널/윤문 실행 진입) | §2.2 | F-017, F-025 | — |
| **S-300** | S3 캐릭터 갤러리(화면) | §2.3 | F-009~F-011 | A-014~A-022 |
| S-301 | 캐릭터 카드 그리드 | §2.3 | F-009 | A-014, A-015 |
| S-302 | 캐릭터 상세 Sheet(7필드 편집) | §2.3 | F-009, F-011 | A-016, A-017, A-018, A-019 |
| S-303 | 관계 편집 섹션(텍스트 라벨 — G4 Q2) | §2.3 | F-010 | A-020, A-021, A-022 |
| **S-400** | S4 세계관/로어북(화면) | §2.4 | F-012~F-016 | A-023~A-027 |
| S-401 | 로어북 리스트·카테고리 필터·검색창 | §2.4 | F-012, F-013, F-015 | A-023, A-025, A-026 |
| S-402 | 로어북 상세 Sheet(키워드 태그 포함) | §2.4 | F-012, F-014 | A-024, A-026, A-027 |
| S-403 | 참조 회차 Collapsible | §2.4 | F-016 | A-025 |
| **S-500** | S5 AI 패널(Sheet) | §2.5 | F-017, F-019~F-024 | A-028~A-033 |
| S-501 | 호출 컨텍스트 폼(엔드포인트/모델/temperature/max_tokens) | §2.5 | F-017, F-023 | A-028, A-030, A-031 |
| S-502 | 프롬프트 프리셋 선택 + 직접 입력 | §2.5 | F-019 | A-032 |
| S-503 | 컨텍스트 체크박스 + NFR-201 전송 고지 Alert | §2.5 | F-020 | A-033 |
| S-504 | 스트리밍 출력 영역 + 오류 Alert(3종 한국어) | §2.5 | F-021, F-024 | A-033 |
| S-505 | 끼워넣기/선택 교체/복사 버튼(P1) | §2.5 | F-022 | — |
| **S-600** | S6 윤문 리포트(Dialog) | §2.6 | F-025~F-031 | A-034~A-037 |
| S-601 | 변경률 게이트 바(30% 경고/50% 차단) | §2.6 | F-029 | A-034 |
| S-602 | 병렬 diff 뷰(jsdiff 문자 단위) | §2.6 | F-028 | A-034, A-035 |
| S-603 | 진단 span 하이라이트 + 카테고리 패널(taxonomy A~J) | §2.6 | F-027 | A-034, A-035 |
| S-604 | 수락/거절/재실행 컨트롤 + 윤문 강도 선택(route_hint) | §2.6 | F-026, F-028, F-030 | A-034, A-036, A-037 |
| S-605 | 윤문 실행 이력(Collapsible) | §2.6 | F-031 | A-035 |
| S-606 | 윤문 실행 진행 상태(900s 동기 대기 UI — G4 Q4) | §2.6 | F-025 | A-034 |
| **S-700** | S7 설정(화면) | §2.7 | F-017, F-018, F-023, F-030, F-032~F-034, F-036 | A-028~A-032, A-038 |
| S-701 | AI 엔드포인트 탭(api_key 마스킹만 노출) | §2.7 | F-017, F-018, F-023 | A-028, A-029, A-030, A-031 |
| S-702 | 윤문 탭(강도·게이트 임계값·명령 프리셋 — G4 Q1) | §2.7 | F-025, F-026, F-029, F-030 | A-032 |
| S-703 | 테마·타이포그래피 탭(+시니어 배려 모드 토글) | §2.7 | F-035 | — |
| S-704 | 규정·현황 탭(NFR-404 안내 고정) | §2.7 | F-032, F-033 | A-038 |
| S-705 | 백업·복원 탭(파일 단위 수동 — G4/A-039) | §2.7 | F-034 | — (A-039: 엔드포인트 없음) |
| S-706 | 고급 탭(자동저장 주기·에디터 줄 번호) | §2.7 | F-006 | — |
| S-707 | About / 오픈소스 라이선스 고지 | §2.7 | F-036 | — |

> **매핑 누락 검증**: F-001~F-036 전 건이 위 표에 1건 이상 등장하며(검증 스크립트 기준 누락 0건), A-001~A-040 전 건이 참조된다(A-039는 엔드포인트 미제공 결정으로 S-705에 "—" 표기).

---

## 1. 전체 레이아웃 구조 (사양 §6 3분할 레이아웃)

### 1.1 데스크톱 우선 (≥1280px) — 기본 화면

```
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│ TopBar  ▸ 로고(Jippeel) │ 프로젝트 셀렉터(Dropdown) │ 자동저장 표시(FR-106) │ [설정 ⚙]    │ 56px
├────────────────┬─────────────────────────────────────────────────────┬───────────────────┤
│                │                                                     │                   │
│  LeftSidebar   │                  CenterStage                        │  RightPanel       │
│   (사양 §6)    │                                                     │   (사양 §6)       │
│                │                                                     │                   │
│  회차 트리      │     [S2 에디터] 또는 [S3 갤러리] 또는 [S4 로어북]      │   Sheet/Drawer    │
│  (volume/      │                                                     │   (겹쳐 열림)     │
│   chapter)     │     ┌───────────────────────────────┐               │                   │
│                │     │   메인 컨텐츠 (max-w 720px)     │               │   S5 AI 패널      │
│  ◉ 1권         │     │   - 에디터 (CodeMirror 6)      │               │   또는            │
│    ○ 1화       │     │   - 미리보기 탭                │               │   S6 윤문 리포트  │
│    ○ 2화       │     │                               │               │                   │
│    ○ 3화 ★     │     │   헤더: 제목 + 상태칩 + 메모    │               │   (기본 닫힘,     │
│  ◉ 2권         │     │   푸터: 글자수(공백제외) + 저장  │               │    단축키로 토글) │
│    ○ 4화       │     └───────────────────────────────┘               │                   │
│  [+ 회차 추가]  │                                                     │                   │
│                │                                                     │                   │
│  너비 240px     │                                                     │   너비 0/360/480   │
│  (축소 시 56px) │                                                     │   (열렸을 때)      │
├────────────────┴─────────────────────────────────────────────────────┴───────────────────┤
│ StatusBar  ▸ 현재 회차 · 단어 {n} · 저장 상태(FR-106) · AI OFF/READY/STREAMING           │ 32px
└──────────────────────────────────────────────────────────────────────────────────────────┘
```

**S1 홈(프로젝트 목록)**은 CenterStage만 풀폭으로 사용:
```
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│ TopBar  ▸ 로고 │ [+ 새 프로젝트]   검색  [설정 ⚙]                                          │ 56px
├────────────────┬─────────────────────────────────────────────────────┬───────────────────┤
│  LeftSidebar   │  S1: 프로젝트 카드 그리드 (3열, 카드 240×180)         │  (RightPanel 닫힘) │
│  (필터:        │  ┌────────┐ ┌────────┐ ┌────────┐                      │                   │
│   전체/        │  │ 작품A  │ │ 작품B  │ │ 작품C  │   [+ 새 작품]        │                   │
│   장르/        │  │ 회차 12 │ │ 회차 3 │ │ 회차 0 │                      │                   │
│   최근 작업)   │  └────────┘ └────────┘ └────────┘                      │                   │
│   240px        │                                                     │                   │
├────────────────┴─────────────────────────────────────────────────────┴───────────────────┤
│ StatusBar  ▸ 총 {N}개 작품 · 최근 작업: {lastProjectName}                                  │ 32px
└──────────────────────────────────────────────────────────────────────────────────────────┘
```

**S7 설정**은 CenterStage 풀폭 폼 페이지:
```
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│ TopBar  ▸ 로고 │ (프로젝트 셀렉터 비활성)                    [← 뒤로]                  │ 56px
├────────────────┬─────────────────────────────────────────────────────┬───────────────────┤
│  LeftSidebar   │  S7: 설정 (Tabs)                                    │  (닫힘)           │
│  (설정 카테고리│  ┌─ 탭: AI 엔드포인트 │ 윤문 · 플랫폼 · 테마 · 백업 ┐│                   │
│   네비)        │  │  Form 카드: EndpointList, default_endpoint, ...   ││                   │
│   200px        │  └─────────────────────────────────────────────────┘│                   │
├────────────────┴─────────────────────────────────────────────────────┴───────────────────┤
│ StatusBar  ▸ 설정 변경은 자동저장(별도 표시)                                              │ 32px
└──────────────────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 반응형 브레이크포인트

| 구간 | 너비 | LeftSidebar | CenterStage | RightPanel | 사용 시나리오 |
|------|------|-------------|-------------|------------|--------------|
| **XL** (기본) | ≥1280px | 240px 풀폭 | 가변 (max 720px 컨텐츠) | Sheet 480px 오버레이 | 데스크톱 (1순위 타깃) |
| **L** | 1024~1279px | 240px | 가변 | Sheet 420px 오버레이 | 노트북 |
| **M** | 768~1023px | 56px 축소 (아이콘만) | 가변 | Sheet 380px 풀높이 | 태블릿 |
| **S** | <768px | 숨김 (Drawer) | 풀폭 | 풀폭 Sheet | **모바일 미지원 — 단순 열람 폴백**(에디터/윤문/S5는 사용 권장 안 함, 요구사항 §2.3·§5.2 MVP 제외 — m-6) |

CSS: Tailwind `md:`(768) / `lg:`(1024) / `xl:`(1280) 기준.

> **<768px(S 구간) 처리 방침 — 모바일 미지원·단순 열람 폴백 (m-6)**
> - **MVP 범위 제외**: 요구사항 §2.3·§5.2에서 모바일 사용자는 명시적 제외(MVP는 데스크톱 우선). 본 구간은 우연한 모바일 접속 시 **읽기 전용 폴백**으로만 동작한다.
> - **에디터(S2)·윤문(S6)·AI 패널(S5)·설정 폼(S7)**: 768px 미만에서는 `<Alert variant="warning">` 배너를 화면 상단에 고정 표시하고 핵심 인터랙션(저장·수락·AI 호출)은 비활성. 회차 목록·캐릭터·로어북은 스크롤 열람만 허용.
> - **스타일 가드**: `useMediaQuery('(min-width: 768px)')`로 분기 → false일 때 S2/S3/S4/S5/S6 진입 시 데스크톱 안내 모달 + "데스크톱으로 열기" 안내.
> - **추후 정책 변경 시**: 모바일 지원이 결정되면 본 섹션을 별도 버전(v2.0)에서 정의한다.

### 1.3 전역 컴포넌트 트리

```
<App>
├── <ThemeProvider>                  ← next-themes (다크/라이트 토글)
├── <QueryClient>                    ← TanStack Query (서버 상태)
├── <TooltipProvider delayDuration={300}>
├── <TopBar />
├── <LeftSidebar />                  ← 회차 트리 / 프로젝트 필터 / 설정 네비
│   ├── <ChapterTree />              ← S2 (Radix Collapsible + 커스텀)
│   ├── <ProjectFilterList />        ← S1
│   └── <SettingsNav />              ← S7
├── <CenterStage>
│   ├── <HomePage />                 ← S1
│   ├── <EditorPage />               ← S2
│   ├── <CharacterGalleryPage />     ← S3
│   ├── <LorebookPage />             ← S4
│   └── <SettingsPage />             ← S7
├── <RightPanelSheet />              ← S5/S6 오버레이 (단일 인스턴스, mode로 스위치)
│   ├── <AiPanel mode="ai" />        ← S5
│   └── <RefineReport mode="refine" /> ← S6
├── <StatusBar />
└── <Toaster />                      ← shadcn/ui sonner
```

---

## 2. 화면별 와이어프레임 (S1~S7)

> **공통 표기**
> - `[Btn]` = shadcn `<Button>`
> - `[Inp]` = `<Input>`, `[TA]` = `<Textarea>`, `[Sel]` = `<Select>`
> - `[Sh]` = `<Sheet>`, `[Dlg]` = `<Dialog>`, `[Tab]` = `<Tabs>`, `[Bdg]` = `<Badge>`
> - `[Form]` = shadcn `<Form>` (RHF + zod)
> - `★` = 상태 칩(초고=회색 / 수정중=앰버 / 완료=에메랄드)

---

### 2.1 S1 — 홈 / 프로젝트 목록 (`/`) — **S-100**

```
┌──────────────────────────────────────────────────────────────────┐
│  Jippeel                                  [🔍 검색]   [⚙ 설정]   │  ← TopBar
├────────────┬─────────────────────────────────────────────────────┤
│  필터      │  내 프로젝트                          [+ 새 작품]    │
│  ▸ 전체    │  ┌────────────┐ ┌────────────┐ ┌────────────┐       │
│  ▸ 장르    │  │ 📕 제목    │ │ 📗 제목    │ │ 📘 제목    │       │
│  ▸ 최근    │  │ 판타지      │ │ 무협        │ │ 로맨스      │       │
│            │  │ 12회차      │ │ 3회차       │ │ 0회차       │       │
│  ▼ 필터링   │  │ ★ 수정중    │ │ ★ 초고      │ │ -           │       │
│   ▢ 진행중  │  │ 마지막: 1시간│ │ 마지막: 1일  │ │ -           │       │
│   ▢ 완료    │  │             │ │             │ │             │       │
│            │  │ [열기 →]    │ │ [열기 →]    │ │ [+ 생성]    │       │
│  240px     │  └────────────┘ └────────────┘ └────────────┘       │
│            │                                                     │
│            │  ──── 규정·현황 (FR-601/602) ────                   │
│            │  📄 노벨피아 AI 규정 요약 (확인일: 2026-08-25) [→]  │
│            │  📄 문피아 AI 규정 요약 (확인일: 2026-08-25) [→]    │
│            │                                                     │
│            │  최근 작업 회차 (FR-108)                              │
│            │  ▸ 작품A · 5화 · 2시간 전 (저장됨)         [이어쓰기] │
│            │  ▸ 작품B · 1화 · 1일 전 (저장됨)           [이어쓰기] │
│            │                                                     │
└────────────┴─────────────────────────────────────────────────────┘
```

| 영역 | 설명 | shadcn 컴포넌트 | FR/NFR |
|------|------|----------------|--------|
| 프로젝트 카드 | 240×180, 썸네일·장르·회차수·**회차 상태 집계 칩**·마지막 작업 시간. 호버 시 그림자 강조(transition 짧게) | `<Card>`, `<Badge>`, `<Button>` | **FR-101, FR-102, FR-108** |
| 검색 | 제목·시놉시스 부분일치(서버 API `GET /projects?q=`) | `<Input type="search">` | **FR-101, FR-108** |
| 필터 | 좌측 사이드바 — 장르 멀티셀렉트 + 진행중/완료 체크 | `<Checkbox>`, `<Select>` | **FR-101, FR-105** |
| 새 작품 | Dialog → 제목/장르/시놉시스 입력 → POST → 자동 S2 이동 | `<Dialog>`, `<Form>`, `<Input>`, `<Textarea>` | FR-101 |
| 카드 메뉴 | "열기 / 이름변경 / 복제 / 삭제" (삭제 confirm Dialog) | `<DropdownMenu>`, `<AlertDialog>` | FR-101 |
| 규정 요약 | 카드형 작은 박스, 마지막 확인일 + 외부 링크. NFR-403(확인 시점 표시) | `<Card>`, `<Alert variant="info">` | FR-601, NFR-403 |
| 노벨피아 PLUS 충족 | "프로젝트 내 회차 수 {N}회 / 15회 이상 {✓/✗} · 완료 회차 공백제외 3,000자 {✓/✗}" — **G4 Q3 확정 해석: 하나의 프로젝트 내 회차 수 기준** | `<Progress>` (S-104) | F-033, A-038 |
| 최근 작업 회차 | 클릭 시 S2로 deep-link (`/projects/{id}/write?chapter={cid}`) | `<Button variant="ghost">` | FR-101, FR-108 |
| 빈 상태 | "아직 작품이 없어요. [+ 새 작품]으로 시작" + 일러스트 (저채도 라인) | `<EmptyState>`(커스텀) | **FR-101** |

**빈 상태 / 로딩 / 에러**
- 로딩: 카드 그리드 자리에 `<Skeleton>` 6개
- 에러: 상단 `<Alert variant="error">` (FR-408)
- 빈 상태: 일러스트 + "새 작품" CTA 1개만

> **상태 칩(★) 데이터 출처 명시 (m-3)** — Project 엔티티 자체에는 `status` 필드가 없다. 카드 상태 칩은 **회차 상태 집계**(`status='done'` 회차 비율)로 산출:
> - 집계 쿼리: `GET /projects/{pid}/status` → `{ draft, revising, done, total }` (백엔드 v0.3 §5에 추가 예정)
> - 표시 규칙: `done == total && total > 0` → 완료(에메랄드) / `revising > 0` → 수정중(앰버) / 그 외 → 초고(회색)
> - 데이터 출처 표기: 카드 호버 시 `<Tooltip>` "상태: 회차 {done}/{total} 완료"로 명시.

---

### 2.2 S2 — 회차 에디터 (메인) (`/projects/{id}/write`) — **S-200**

```
┌──────────────────────────────────────────────────────────────────────────┐
│ Jippeel │ 작품A ▼ │ 1권 3화: "__"  [상태: 수정중 ▼]  [⋯ 메뉴]  [💾 저장됨 1분전] │ 56px
├────────────┬────────────────────────────────────────┬────────────────────┤
│ 회차 트리    │ ┌────────────────────────────────────┐ │ (RightPanel 닫힘)  │
│            │ │ 제목 [_____________]  메모 [📝]      │ │                    │
│            │ │ [⋯ 메뉴: 회차 내보내기(.txt/.md) │    │ │                    │
│            │ │        프로젝트 내보내기(.txt/.md)]   │ │                    │  ← M-1 FR-109 / 사양 v0.3 §3 S2
│ ▼ 1권      │ │ ────────────────────────────────    │ │  [▶ AI 패널]       │
│   ○ 1화     │ │                                    │ │  [🔍 윤문 실행]    │
│   ○ 2화     │ │  # 마크다운 본문 (CodeMirror 6)     │ │                    │
│   ● 3화 ★  │ │  ────────────────                  │ │                    │
│   ＋ 추가   │ │  ## 1. 시작                       │ │                    │
│            │ │  줄글 ...                          │ │                    │
│ ▼ 2권      │ │  ────────────────                  │ │                    │
│   ○ 4화     │ │                                    │ │                    │
│            │ │  (가상 스크롤 — NFR-101)            │ │                    │
│            │ │                                    │ │                    │
│            │ │ ──────────────────────────────────  │ │                    │
│ 240px      │ │ [📝 편집]  [👁 미리보기]  [⚙ 설정]    │ │                    │
│            │ └────────────────────────────────────┘ │                    │
│            │ 글자 {총} · 공백제외 {2,847} · 회차 상태 │ │                    │
│            │ 자동저장: ON · 다음 저장: 12초          │ │                    │
└────────────┴────────────────────────────────────────┴────────────────────┘
```

| 영역 | 설명 | shadcn 컴포넌트 | FR/NFR |
|------|------|----------------|--------|
| **회차 트리 (좌)** | 권(volume) 단위 Collapsible. 드래그로 순서/권 이동, 우클릭 메뉴(복제/삭제/이동) | `<Collapsible>` (Radix) + 커스텀 | FR-102, FR-107 |
| **상태 칩** | 초고/수정중/완료 3상태 — 클릭 시 Select | `<Select>`, 색은 토큰(§4) | FR-105 |
| **에디터 (중앙)** | CodeMirror 6, lang-markdown, **lineNumbers 기본 OFF(집중)** + S7 고급 탭에서 토글(m-7, Compartment로 런타임 전환), 테마는 토큰 적용, 폰트는 §4 세리프, 행간 1.85, 자간 +0.01em | `@codemirror/view` 커스텀 | FR-103, NFR-101 |
| **미리보기 탭** | markdown-it 15.x → DOMPurify sanitize → XSS 안전, CodeMirror와 같은 영역에 탭 전환 | `<Tabs>`, `markdown-it`, `dompurify` | FR-103 |
| **글자 수 푸터** | "글자 {총} · **공백제외 {2,847}**" — 공백제외는 굵게·모노스페이스(FR-104) | `<footer>` + 토큰 | FR-104 |
| **저장 상태** | "저장됨 · 방금 전" / "저장 중…" / "저장 실패 (재시도)" — 3초마다 상대시간 갱신, NFR-204(비정상 종료 시 마지막 커밋까지 보존) | `<span>` + `Intl.RelativeTimeFormat` | FR-106, NFR-204 |
| **자동 저장 인디케이터** | "자동저장: ON · 다음 저장: 12초" — 디바운스 1.5s + 서버 PATCH | `<Progress>`(작은 막대) | FR-106 |
| **에디터 메뉴 (M-1)** | 헤더 [⋯ 메뉴] 클릭 → DropdownMenu
- 회차 내보내기 → `.txt` / `.md` 선택 (현재 회차만)
- 프로젝트 내보내기 → `.txt` / `.md` 선택 (전 회차 zip 또는 묶음 파일, FR-109)
내보내기는 텍스트 변환(마크다운→plain) 후 다운로드, 외부 호출 없음 | `<DropdownMenu>`, `JSZip`(회차 묶음 시) | **FR-109** (사양 v0.3 §3 S2) |
| **회차 메모** | 헤더 옆 [📝] 클릭 → Popover에 메모 입력(별도 필드, `chapter.memo`) | `<Popover>`, `<Textarea>` | FR-108 |
| **빠른 액션 (우측 고정 FAB)** | `[▶ AI 패널]` `[🔍 윤문 실행]` — 항상 노출, 단축키 `Alt+A` / `Alt+R` | `<Button size="icon">` | **NFR-303** (m-2: 로어북 키워드 FR-303 오참조 제거) |
| **회차 삭제** | 트리 우클릭 → 삭제 confirm `<AlertDialog>` | `<AlertDialog>` | FR-102 |
| **본문 빈 상태** | "마크다운으로 첫 회차를 시작하세요. 단축키 `Ctrl+Alt+H`" | `<EmptyState>` | **FR-101, FR-103** (m-1: 로어북 FR-301 오참조 정정) |

**에디터 확장(CodeMirror 6)** — 사양 §6 · §2.1:
- `@codemirror/view` `EditorView` + `keymap.of([...])` 단축키
- `@codemirror/state` `EditorState` + `Compartment` (테마·읽기전용 토글)
- `@codemirror/lang-markdown`
- `@codemirror/commands` (Ctrl+D 라인 삭제 등)
- `@codemirror/search` (내장 검색)

**단축키 (S2)**: §6 표 참조

---

### 2.3 S3 — 캐릭터 갤러리 (`/projects/{id}/characters`) — **S-300**

```
┌──────────────────────────────────────────────────────────────────────────┐
│ Jippeel │ 작품A ▼                                  [💾 저장됨]          │
├────────────┬────────────────────────────────────────┬────────────────────┤
│ 필터       │ 캐릭터 ({n})    [+ 캐릭터 추가]          │  (Sheet로 상세)    │
│ ▢ 전체     │ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │                    │
│ ▢ 주연     │ │ 아바타 │ │ 아바타 │ │ 아바타 │ │ 아바타 │   │  ┌─────────────┐  │
│ ▢ 조연     │ │  이름 │ │  이름 │ │  이름 │ │  이름 │   │  │ 이름 [____] │  │
│ ▢ 적대     │ │  역할 │ │  역할 │ │  역할 │ │  역할 │   │  │ 별칭 [...   │  │
│            │ │  ●주연│ │  ●주연│ │  ●조연│ │  ●적대│   │  │ 역할 [주연▼]│  │
│ 검색 🔍     │ └──────┘ └──────┘ └──────┘ └──────┘   │  │ ─────────  │  │
│            │                                        │  │ 외형 [____] │  │
│ 240px      │ 관계 (FR-203)                          │  │ 성격 [____] │  │
│            │ ┌─────┐→"주군-가신"←┌─────┐            │  │ 말투 [____] │  │
│            │ │ 주연1│            │ 조연1│            │  │ 배경 [____] │  │
│            │ └─────┘             └─────┘            │  │ 관계 [+ 추가]│  │
│            │ [+ 관계 추가]                           │  │ [저장] [닫기]│  │
│            │                                        │  └─────────────┘  │
└────────────┴────────────────────────────────────────┴────────────────────┘
```

| 영역 | 설명 | shadcn 컴포넌트 | FR/NFR |
|------|------|----------------|--------|
| 카드 그리드 | 4열(L) / 3열(M) / 2열(S), 200×240, 썸네일+이름+역할 Badge | `<Card>`, `<Badge>`, `<Avatar>` | FR-205 |
| 카드 클릭 | 우측 Sheet 슬라이드 인, 상세 편집(FR-205 "그리드 + 드로어") | `<Sheet side="right">` | FR-205 |
| 검색 | 이름·별칭·역할 부분일치, 디바운스 300ms | `<Input type="search">` | FR-201 |
| 필터 | 역할 멀티셀렉트(주연/조연/적대/기타) | `<Select>`, `<Checkbox>` | FR-202 |
| 캐릭터 추가 | Sheet를 "빈 폼" 상태로 열기 | `<Sheet>`, `<Form>` | FR-201 |
| **드래그 정렬** | `dnd-kit` (백로그) — MVP는 순서 필드 PATCH | (백로그) | — |
| 관계 편집 | Sheet 하단 "관계" 섹션 — 캐릭터 B 선택 + 라벨 텍스트 | `<Select>`, `<Input>`, `<Button>` | FR-203 |
| SillyTavern 호환 | 폼 데이터는 `card_json` JSON으로 함께 저장, PATCH 시 동기화 (FR-204) | (백엔드 처리) | FR-204 |
| **AI 사용 시** | Sheet 안 [AI로 초안 생성] → S5 호출, 결과는 **자동 삽입 안 함** (P1) — "외형 필드에 끼워넣기" 버튼으로 명시 | `<Button variant="outline">` | FR-406 |

**Sheet 폼 필드 (FR-202)**: 이름*, 별칭[], 역할*, 외형, 성격, 말투, 배경. 모두 `<Form>` + zod.

**관계 표기 (MVP)**: 텍스트 라벨 수준 (예: "주군-가신"), 그래프 뷰는 백로그. (FR-203)

---

### 2.4 S4 — 세계관 / 로어북 (`/projects/{id}/lore`) — **S-400**

```
┌──────────────────────────────────────────────────────────────────────────┐
│ Jippeel │ 작품A ▼                                  [💾 저장됨]          │
├────────────┬────────────────────────────────────────┬────────────────────┤
│ 카테고리    │ 로어북 ({n})    [+ 항목 추가]           │  (Sheet 상세)      │
│ ▢ 전체     │ ┌─────────────────────────────────┐    │  ┌─────────────┐  │
│ ▢ 용어     │ │ 용어  · "마력석"                  │    │  │ 제목 [____] │  │
│ ▢ 장소     │ │ 키워드: [마력][광물][전쟁]         │    │  │ 분류 [용어▼]│  │
│ ▢ 세력     │ │ 본문: 마력석은 ...                │    │  │ 키워드 [...]│  │
│ ▢ 기타     │ │ 참조 회차: 1화, 3화               │    │  │ 본문 [____] │  │
│            │ │ [수정] [삭제]                      │    │  │ [저장] [닫기]│  │
│ 검색 🔍     │ └─────────────────────────────────┘    │  └─────────────┘  │
│            │ ┌─────────────────────────────────┐    │                    │
│ 240px      │ │ 장소 · "왕도 아르칸"              │    │                    │
│            │ │ 키워드: [수도][도시]              │    │                    │
│            │ │ ...                                │    │                    │
│            │ └─────────────────────────────────┘    │                    │
└────────────┴────────────────────────────────────────┴────────────────────┘
```

| 영역 | 설명 | shadcn 컴포넌트 | FR/NFR |
|------|------|----------------|--------|
| 카테고리 필터 | 4종(용어/장소/세력/기타) — `Tabs`(수직) 또는 사이드 리스트 | `<Tabs orientation="vertical">` 또는 `<RadioGroup>` | FR-302 |
| 검색 | 키워드·제목·본문 (서버 FTS5 — NFR-103 1초 이내) | `<Input type="search">` | FR-304, NFR-103 |
| 키워드 태그 | 칩 형태 `<Badge variant="secondary">` × N, 클릭 시 동일 키워드 항목 필터 | `<Badge>`, `<Popover>` | FR-303 |
| 추가/수정 | Sheet 우측, 카테고리/제목/키워드/본문 | `<Sheet>`, `<Form>` | FR-301 |
| 참조 회차 표시 | (선택) 해당 항목 키워드가 등장한 회차 목록 (FR-305) | `<Collapsible>` | FR-305 |
| **AI 사용** | Sheet 안 [AI로 본문 다듬기] → S5 호출, 결과는 **자동 삽입 안 함** (P1) | `<Button variant="outline">` | FR-406 |
| 빈 상태 | "세계관의 첫 조각을 등록하세요" | `<EmptyState>` | — |

---

### 2.5 S5 — AI 패널 (전역 사이드 패널) — **S-500**

> **호출 지점**: S2/S3/S4 어디서든 `Alt+A` 또는 우측 FAB → 우측에서 Sheet 슬라이드 인.
> **원칙 (P1)**: 결과는 본문/필드에 절대 자동 삽입되지 않으며, "끼워넣기/선택 교체/복사" 명시 버튼으로만 반영(FR-406).

```
┌──────────────────────────────────────────┐  ← Sheet 480px (L) / 420px (M) / 380px (S)
│  AI 어시스턴트                          [×] │ 32px 헤더
├──────────────────────────────────────────┤
│ ⚠ AI 결과는 자동으로 본문에 들어가지     │  ← P1 Alert (FR-406 · 모든 시트 상단 고정)
│   않습니다. 끼워넣기/선택 교체만 반영.    │
│ ──────────────────────────────────────── │
│ 📡 선택한 회차·카드·로어북 내용은 지정한  │  ← NFR-201 전송 고지 (M-2 · 사양 v0.3 §3 S5)
│    LLM 엔드포인트로 전송됩니다.           │
├──────────────────────────────────────────┤
│ ┌─ 호출 컨텍스트 ──────────────────────┐ │
│ │ 엔드포인트:   [LM Studio ▼] (기본)    │ │  ← AiEndpoint Select
│ │ 모델:        [qwen2.5-7b ▼]          │ │  ← GET /ai/endpoints/{id}/models 결과
│ │ temperature: [━━━●━━━] 0.7           │ │  ← Slider
│ │ max_tokens:  [━━━●━━━] 2048         │ │  ← Slider
│ └──────────────────────────────────────┘ │
│                                          │
│ ┌─ 프롬프트 ─────────────────────────┐  │
│ │ 프리셋: [장면 생성 ▼]  [⚙ 관리]     │  │  ← PromptPreset Select
│ │ 프롬프트 직접 입력:                  │  │  ← Textarea
│ │ ┌────────────────────────────────┐  │  │
│ │ │ {prompt_template}                │  │  │
│ │ └────────────────────────────────┘  │  │
│ └──────────────────────────────────────┘ │
│                                          │
│ ┌─ 포함 컨텍스트 (FR-404) ────────────┐  │
│ │ ☑ 현재 회차 (3화 · 2,847자)          │  │  ← Checkbox
│ │ ☐ 선택 캐릭터 (2)                   │  │
│ │ ☐ 선택 로어북 (0)                   │  │
│ │ 선택 캐릭터/로어북: [Chip] [Chip]    │  │  ← Badge ×
│ └──────────────────────────────────────┘ │
│                                          │
│            [ ✨ 생성 시작 ]              │  ← 큰 Primary Button
│                                          │
│ ── 응답 (스트리밍, FR-405) ──────────   │
│ ┌──────────────────────────────────────┐ │
│ │ 토큰이 실시간으로 흘러옵니다...        │ │
│ │ ─── (스트리밍 SSE) ───                │ │
│ │ ···                                 │ │
│ │                                       │ │  ← 코드블록 + monospace
│ │                                       │ │  ← 자동 스크롤 (sticky bottom)
│ └──────────────────────────────────────┘ │
│                                          │
│ [↪ 끼워넣기] [⤳ 선택 교체] [⧉ 복사]    │  ← 결과 도착 후 활성
│                                          │
│ 상태: STREAMING · 12.4 tok/s · 1,024 t  │  ← FR-405, FR-408
└──────────────────────────────────────────┘
```

| 영역 | 설명 | shadcn 컴포넌트 | FR/NFR |
|------|------|----------------|--------|
| 호출 컨텍스트 | 엔드포인트/모델/온도/최대토큰 | `<Select>`, `<Slider>`, `<Form>` | FR-401, FR-407 |
| 모델 목록 | 백엔드 `GET /ai/endpoints/{eid}/models` 결과를 캐시(5분) | TanStack Query | FR-401 |
| 엔드포인트 관리 | "관리" 버튼 → S7 설정의 AI 탭으로 이동(deep-link) | `<Button variant="link">` | FR-401 |
| 프리셋 | 빌트인(장면 생성·대사 보강·요약·이어쓰기) + 사용자 정의 | `<Select>` | FR-403 |
| 컨텍스트 체크 | 현재 회차 / 선택 캐릭터 / 선택 로어북 포함 여부 | `<Checkbox>` | FR-404 |
| 결과 스트리밍 | `fetch` 스트림(SSE, `ReadableStream`) → 청크 단위 append, 자동 스크롤 | `<ScrollArea>`, `lib/aiStream.ts`(`fetch`+`getReader()`) | FR-405, NFR-102 |
| **전송 고지 (M-2)** | Sheet 상단 P1 Alert 아래에 두 번째 Alert 고정 — "선택한 회차·카드·로어북 내용은 지정한 LLM 엔드포인트로 전송됩니다"(NFR-201). 사용자가 컨텍스트 체크 ON 시 매 호출마다 명시적으로 인지하도록 항상 노출 | `<Alert variant="info" icon={<CloudUpload />}>` | **NFR-201** (사양 v0.3 §3 S5) |
| **P1 액션 버튼** | "끼워넣기" → 현재 회차 본문 끝에 append, "선택 교체" → 에디터에서 선택한 범위만 교체, "복사" → 클립보드. **세 버튼 모두 명시적 사용자 클릭 필요** | `<Button>`, `navigator.clipboard` | FR-406, **P1** |
| 끼워넣기 확인 | S2 에디터로 토글되며 하이라이트(1.5s 깜빡임) + 토스트 "에디터에 추가됨" | `<Toast>` | FR-406 |
| 에러 안내 | 엔드포인트 불일치/인증 실패/타임아웃/네트워크 — `<Alert variant="error">` + 재시도 버튼 | `<Alert>`, `<Button>` | FR-408 |
| Ollama 안내 | 엔드포인트 선택지에 "Ollama 네이티브 미지원 (OpenAI 호환만)" 안내 (FR-409) | `<Tooltip>` | FR-409 |
| 스트리밍 중 UX | 입력/에디터는 **활성 유지** (NFR-102 멈춤 금지). 우측 상단에 작은 "STREAMING ●" 뱃지 | `<Badge variant="info">` | NFR-102 |
| 토큰/속도 표시 | 백엔드 SSE의 usage 메타 또는 클라이언트 측정(글자수/tick) | `<span>` | — |

**S5 호출 진입점**
- S2 에디터 헤더의 [✨ AI 어시스턴트] 버튼
- S3 캐릭터 Sheet 안 [AI로 초안 생성] 버튼
- S4 로어북 Sheet 안 [AI로 본문 다듬기] 버튼
- 전역 단축키 `Alt+A`
- 호출 시 호출 위치 컨텍스트가 자동 체크됨(현재 회차/Sheet의 캐릭터/로어북)

---

### 2.6 S6 — 윤문 리포트 (M5) — **S-600**

> **호출 지점**: S2 에디터의 [🔍 윤문 실행] 버튼 또는 `Alt+R`.
> **원칙 (P4)**: diff는 원문/수정본 병렬 + 변경 하이라이트, 진단 span은 카테고리별 색상, 변경률 게이트(30% 경고/50% 차단) 시각화, **사용자 수락/거절 결정**(자동 덮어쓰기 없음).

```
┌──────────────────────────────────────────────────────────────────────────┐
│  윤문 리포트                                                  [×]        │
├──────────────────────────────────────────────────────────────────────────┤
│ 회차: 1권 3화 · 강도: standard (자동) · 진단 {23} span · 변경률 27.4%    │
│ ┌────────────────────────────────────────────────────────────────────┐  │
│ │ 변경률 게이트 (FR-505)                                              │  │
│ │ ████████████████████████░░░░░░░░░  27.4% / 30% 경고선              │  │
│ │ ℹ 30% 미만 — 수락 가능 / 30~50% 경고 / 50% 이상 차단              │  │
│ └────────────────────────────────────────────────────────────────────┘  │
│ ⚠ 본 결과는 "AI 티 다듬기" 목적입니다. 탐지 회피를 의미하지 않습니다.    │  ← FR-506 / NFR-401
│   [윤문 실행 옵션: light | standard | heavy]   [🔄 다시 실행]            │
│                                                                          │
│ [병렬 diff 뷰]   (좌: 원문 / 우: 수정본)                                  │
│ ┌─────────────────────────────┬──────────────────────────────────────┐  │
│ │ 원문 (2,847자)               │ 수정본 (2,063자)                      │  │
│ │ ────────────                │ ────────────                          │  │
│ │ ## 1. 시작                  │ ## 1. 시작                            │  │
│ │ 어둠 속에서                  │ (어둠 속)[번역투]에서                  │  │  ← diff: 삭제(빨강)/추가(녹색)
│ │ 한 소년이 깨어났다.          │ 한 소년이 (깨어)[번역]났다.            │  │
│ │ …                          │ …                                    │  │
│ │                              │                                      │  │
│ │ ─ 진단 (FR-503) ─           │                                       │  │
│ │ [번역투] line 4-6: "어둠 속에서" (severity: warn)                  │  │  ← 카테고리별 색 underline
│ │ [병렬구조] line 8-12: …                                               │  │
│ └─────────────────────────────┴──────────────────────────────────────┘  │
│                                                                          │
│ [사이드 패널: 진단 카테고리]                                              │
│ • 번역투    ▢ 4건   [회색: 삭제] [녹색: 추가]                             │
│ • 병렬구조  ▢ 2건                                                             │
│ • 영어인용  ▢ 1건                                                             │
│ • …                                                                              │
│                                                                          │
│ ┌────────────────────────────────────────────────────────────────────┐  │
│ │ 변경률 {27.4%} → 수락 가능 (게이트: pass)                             │  │  ← P4 / FR-504
│ │ [  ✓ 수락 (에디터에 반영)  ]  [  ✗ 거절  ]                           │  │
│ │ 수락 시 회차 본문이 수정본으로 교체됩니다. 자동 저장은 유지됩니다.   │  │  ← m-8 단순화
│ │   (accept 완료 후 TanStack Query invalidate로 회차/히스토리 갱신)    │  │
│ │ 거절 시 기록만 남습니다.                                             │  │
│ │ ── 변경률 ≥ 50% 시 (게이트: block) ──                                │  │  ← C-2 · 사양 v0.3 §8.3
│ │ [  ✓ 수락  ] disabled · "50% 초과 — 자동 수락 차단"                  │  │
│ │ [  🔄 윤문 재실행 (강도 변경)  ]  [  🗑 폐기  ]                      │  │  ← 강제 수락 경로 완전 삭제
│ │ ※ 50% 초과 시 본 결과는 반영 불가. 재실행 또는 폐지만 선택 가능.     │  │
│ └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│ 실행 이력 (FR-507) — light(2), standard(1), heavy(0)                    │
│ ▸ 2026-08-25 14:22 · standard · 27.4% · [거절]                          │
└──────────────────────────────────────────────────────────────────────────┘
```

| 영역 | 설명 | shadcn 컴포넌트 | FR/NFR |
|------|------|----------------|--------|
| Dialog / 모달 | 화면 중앙 모달(전용 뷰, FR-503~505·507) — `<Dialog>` 풀폭 | `<Dialog>`, `<ScrollArea>` | — |
| 변경률 게이트 | 3구간 Progress(0~30% pass / 30~50% warn / 50%+ block) | `<Progress>` + 색 토큰 | FR-505 |
| **30% 경고 / 50% 차단 (C-2)** | 30~50%: `<Alert variant="warning">` "과윤문 가능성 — 신중 검토 권장" + 수락 가능. **50% 초과: 수락 버튼 disabled + `<Alert variant="error">` "50% 초과 — 자동 수락 차단" + 강제 수락 경로 없음**. 선택지는 **윤문 재실행(강도 변경)** 또는 **폐기**만 허용(im-not-ai 4대 철칙 #4) | `<Alert>`, `<Button disabled>`, `<Button variant="outline">`(재실행), `<Button variant="ghost">`(폐기) | **FR-505** (사양 v0.3 §8.3) |
| **P4 / FR-506 안내** | "본 결과는 'AI 티 다듬기' 목적입니다. 탐지 회피를 의미하지 않습니다" — 상단 Alert 고정 | `<Alert variant="info">` | FR-506, NFR-401 |
| diff 뷰 | 좌(원문)/우(수정본) **병렬**, 줄 정렬(scroll sync). 변경 토큰(문자 단위) 빨강/녹색. 줄 전체 삭제는 흐림 + 취소선 | `<ScrollArea>`, `jsdiff.diffChars` + 커스텀 | FR-504 |
| 진단 span | 부록05 §⑤-5 규격(`category`/`start`/`end`/`severity`/`message`) — `category`별 색상 underline + hover 시 툴팁 | `<Tooltip>`, `<mark>` | FR-503 |
| **카테고리 색 매핑 (M-4)** | §4.1 `--cat-A`~`--cat-J` 토큰을 **humanize-korean taxonomy ID(A~J)** 기준으로 매핑 — A 번역투 / B 영어인용 / C 구조적패턴 / D 관용구 / E 리듬 / F 수식중복 / G Hedging / H 접속사 / I 형식명사 / J 시각장식. 백엔드 §5 span 스키마의 `category` 값(A~J)을 그대로 받아 색상 매핑 | `<span>` + style | **FR-503** (사양 v0.3 §5) |
| 윤문 강도 선택 | 라디오 [light / standard / heavy], 기본 자동(route_hint 자동) | `<RadioGroup>` | FR-502 |
| 다시 실행 | 현재 텍스트로 재실행 (강도 변경 가능) | `<Button variant="outline">` | FR-501 |
| **수락 버튼 (m-8 단순화)** | 클릭 시 1) `POST /refine/runs/{id}/accept` 2) 회차 본문 교체 3) **자동저장 유지** (일시 OFF 로직 삭제) 4) `queryClient.invalidateQueries(['chapter', cid])` + `['refine-runs', cid]` 5) Dialog 닫힘 + 토스트 "윤문 수락됨". 자동저장 디바운스가 회차 본문 변경을 정상적으로 감지하므로 별도 OFF/ON 윈도우가 불필요 | `<Button>` (default size) | **FR-504, FR-507** |
| **거절 버튼** | `POST /refine/runs/{id}/reject` → 기록만, 본문 무변경 | `<Button variant="ghost">` | FR-504, FR-507 |
| 실행 이력 | 회차 하단 Collapsible — 시간·강도·변경률·수락여부 | `<Collapsible>`, `<Table>` | FR-507 |

**색 토큰 가이드 (윤문 카테고리) — M-4 taxonomy ID(A~J) 기준** — §4.1에서 HSL로 정의
```
A 번역투      --cat-A
B 영어인용    --cat-B
C 구조적패턴  --cat-C
D 관용구      --cat-D
E 리듬        --cat-E
F 수식중복    --cat-F
G Hedging     --cat-G
H 접속사      --cat-H
I 형식명사    --cat-I
J 시각장식    --cat-J
```
> v1에서 발견된 '리듬' 중복 오타(중복 cat-7/카테고리 표시 두 번)는 정정됨. `category` 식별자는 백엔드 §5 span 스키마가 그대로 송출하는 taxonomy ID(A~J 문자열)와 1:1 대응한다.

**윤문 실행 진행 상태 (G4 결정사항 Q4 반영 — v1.3 신설, S-606)**

MVP는 refine을 **동기 대기(최대 900s)로 유지**한다(백그라운드 잡 전환은 백로그). 이에 따라 [🔍 윤문 실행] 클릭 직후 S6 Dialog는 결과 대신 실행 중 상태를 표시한다:

- 스피너 + 경과 시간 타이머("실행 중… 42초 경과") — `setInterval` 1초 갱신
- 안내 문구: "윤문 파이프라인은 최대 900초(15분)까지 소요될 수 있습니다." (강도 heavy일 때 강조)
- 이 구간의 [닫기]는 Dialog만 닫고 실행은 계속되며, 재오픈 시 `GET /refine/runs/{run_id}` 폴링(5초 주차, TanStack Query `refetchInterval`)으로 완료 여부를 확인해 결과로 전환한다
- 502/503 오류 도착 시 즉시 오류 Alert(F-024 한국어 안내)로 전환

**변경률 게이트 시각 규칙 (F-029 / C-2 강화)**:
- `< 30%` (gate: `pass`): progress 1색(에메랄드), "수락 가능" 라벨, [수락] 활성
- `30~50%` (gate: `warn`): progress 앰버(저채도), `<Alert variant="warning">` "과윤문 가능성 — 신중 검토 권장" + [수락] 활성 (사용자 결정권 유지)
- `≥ 50%` (gate: `block`): progress 빨강(저채도), `<Alert variant="error">` "50% 초과 — 자동 수락 차단 — 새로 쓰기를 권장합니다", **[수락] 버튼 disabled + 강제 수락 경로 완전 삭제**(AlertDialog 자체 미노출). 사용자가 선택할 수 있는 액션은 **윤문 재실행(강도 변경)** 또는 **폐기(거절 기록만)** 두 가지뿐 — NFR-401 윤리·사양 v0.3 §8.3·im-not-ai 4대 철칙 #4 준수

---

### 2.7 S7 — 설정 (`/settings`) — **S-700**

```
┌──────────────────────────────────────────────────────────────────────────┐
│ Jippeel                                          [← 뒤로] [💾 저장됨]    │
├────────────┬─────────────────────────────────────────────────────────────┤
│ 설정 카테고리 │  [AI 엔드포인트] [윤문] [테마] [규정·현황] [백업·복원]     │
│ ▸ AI       │  ┌────────────────────────────────────────────────────┐    │
│ ▸ 윤문      │  │  AI 엔드포인트 (FR-401, FR-407)                       │    │
│ ▸ 테마      │  │  ┌────────────────────────────────────────────────┐  │    │
│ ▸ 규정·현황  │  │  │ ● LM Studio (기본)                              │  │    │
│ ▸ 백업      │  │  │   base_url: http://localhost:1234/v1             │  │    │
│ ▸ 고급      │  │ ▸ 고급      │  │  │   api_key: •••••••••••••••• (마스킹 — 저장값 그대로) │  │    │  ← C-1
│            │  │  │   [  🔑 키 변경  ]                              │  │    │  ← [표시] 버튼 완전 삭제
│            │  │  │   ※ 저장된 키는 보안 정책상 마스킹만 노출         │  │    │  ← FR-402/NFR-202
│            │  │  │     (재표시 불가 — 변경하려면 새 값 입력)        │  │    │
│            │  │  │   default_model: qwen2.5-7b                      │  │    │
│ 200px      │  │  │   temperature: 0.7  ·  max_tokens: 2048          │  │    │
│            │  │  │   [테스트 연결] [모델 새로고침] [기본으로 설정]    │  │    │
│            │  │  └────────────────────────────────────────────────┘  │    │
│            │  │  [+ 새 엔드포인트 추가]                                  │    │
│            │  │  ─────────────────────────────────────────────────     │    │
│            │  │  프롬프트 프리셋 (FR-403)                                │    │
│            │  │  ▸ 장면 생성 / 대사 보강 / 요약 / 이어쓰기 (빌트인)      │    │
│            │  │  [+ 사용자 프리셋 추가]                                  │    │
│            │  └────────────────────────────────────────────────────┘    │
└────────────┴─────────────────────────────────────────────────────────────┘
```

| 탭 | 내용 | shadcn 컴포넌트 | FR/NFR |
|---|------|----------------|--------|
| **AI 엔드포인트 (C-1)** | 엔드포인트 CRUD(리스트 + 폼), 기본 표시, **API 키는 마스킹만 노출(재표시 버튼 없음) — 키 변경은 별도 입력 흐름으로만 가능**(FR-402/NFR-202), Ollama 네이티브 미지원 안내(FR-409) | `<Form>`, `<Input type="password">`, `<Dialog>`(키 변경), `<Tabs>` | FR-401·402·407·409, **NFR-202** |
| **윤문 (S-702)** | 기본 강도(light/standard/heavy), 자동 판정 ON/OFF, 게이트 임계값 표시(고정값 30/50 — F-029), **윤문 엔진 명령 프리셋(G4 Q1 — 아래 블록 참조)** | `<RadioGroup>`, `<Switch>`, `<Textarea>` ×2, `<Dialog>`(명령 편집) | F-025·026·029·030, A-032, T-007 |
| **테마** | 다크/라이트/시스템, 세리프 폰트 선택(Pretendard / Noto Serif KR), 행간(1.7/1.8/1.9), 자간 | `<RadioGroup>`, `<Slider>`, `<Select>` | §4 |
| **규정·현황 (M-3)** | 문피아·노벨피아·조아라 AI 규정 요약(확인일 표시 — NFR-403), 노벨피아 PLUS 충족 현황(FR-602), **NFR-404 안내 Alert 고정 표시** — "AI 사용 여부의 공개/비공개 판단은 작가의 몫입니다"(앱은 안내 의무만 부담) | `<Card>`, `<Alert>`, `<Progress>` | FR-601·602, **NFR-403, NFR-404** |
| **백업·복원** | SQLite 파일 단위 백업(.zip: db + 설정), 복원(파일 선택), NFR-203 | `<Button>`, `<Input type="file">` | NFR-203 |
| **고급** | 자동 저장 주기(0.5~5s, 기본 1.5s), **에디터 줄 번호 표시(m-7 — 기본 OFF, Compartment 토글)**, 디버그 로그, 시드 리셋 | `<Slider>`, `<Switch>` | — |

**S7의 윤문 탭**에 윤문 모듈 4대 철칙 + FR-506 문구 명시:
> "윤문은 '품질 다듬기' 목적입니다. AI 탐지 회피를 의미하지 않습니다." (NFR-401)

**윤문 엔진 명령 프리셋 (G4 결정사항 Q1 반영 — v1.3 신설, S-702)**

결정사항_G4.md Q1: "심는다 — 진단·윤문 명령 기본 프리셋 2종을 프리셋 초기값(T-007)에 포함, 사용자 편집 가능. env 미설정 시 503 graceful degrade 유지".

| 항목 | 설계 |
|------|------|
| 프리셋 초기값 | 진단 명령(`IM_NOT_AI_DIAGNOSE_CMD` 대응) · 윤문 명령(`IM_NOT_AI_REFINE_CMD` 대응) **2종을 T-007 PromptPreset 초기 시드에 포함**하여 첫 사용자가 빈 상태로 막히지 않게 한다 |
| placeholder | `{run_dir} {input} {input_combined} {diagnosis} {output}` — 기술설계 §0.4 템플릿 문법 그대로, 예시 값은 `claude -p ...`(claude CLI 템플릿) |
| 사용자 편집 | 윤문 탭 내 [명령 프리셋 편집] Dialog(`<Textarea>` 2개 + placeholder 치환 안내)에서 수정·초기화 가능. 저장 시 A-032(PATCH `/ai/presets/{id}`)로 반영 |
| 미설정 동작 | 명령이 비어 있으면 S6 실행 시 백엔드 503(HumanizeNotConfigured) → S6에 한국어 Alert "윤문 엔진이 설정되지 않았습니다. 설정 → 윤문 탭에서 명령을 등록하세요." + [설정 열기] deep-link 버튼. 정량 지표·게이트 등 로컬 윤문은 계속 동작(graceful degrade) |
| 보안 표기 | 명령 템플릿 입력란 아래 "입력 원고는 파일 경유로 전달되며 셸 특수문자는 자동 이스케이프됩니다" 안내 문구 고정 |

**S7의 규정·현황 탭 와이어프레임 (M-3 추가)**
```
┌────────────────────────────────────────────────────────────────────┐
│  규정·현황 (FR-601·602 / NFR-403·404)                              │
├────────────────────────────────────────────────────────────────────┤
│ ⚠ AI 사용 여부의 공개/비공개 판단은 작가의 몫입니다                │  ← NFR-404 안내 Alert
│   앱은 안내 의무만 부담하며, 권장 방침을 유도하지 않습니다.          │  (탭 진입 시 최상단 고정)
│ ─────────────────────────────────────────────────────────────── │
│ 📄 노벨피아 AI 규정 요약        마지막 확인: 2026-08-25    [→]    │  ← FR-601
│ 📄 문피아 AI 규정 요약          마지막 확인: 2026-08-25    [→]    │
│ 📄 조아라 AI 규정 요약          마지막 확인: 2026-08-25    [→]    │
│ ─────────────────────────────────────────────────────────────── │
│ 노벨피아 PLUS 충족 현황 (F-033 / A-038)                              │
│ 프로젝트 내 회차 수 {N}회 / 15회 이상 {✓/✗} (G4 Q3)                 │  ← Progress
│ 완료 회차 공백제외 3,000자 {✓/✗}                                    │  ← Progress
│ [Progress bar]                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## 3. 핵심 인터랙션 플로우

### 3.1 회차 집필 흐름 (S2 → S5 → S2)

```
[사용자]                  [S2 에디터]                   [S5 AI 패널]                  [백엔드 / AI]
   │                          │                              │                            │
   │ 키보드로 본문 작성         │                              │                            │
   ├─────────────────────────►│                              │                            │
   │                          │ 디바운스 1.5s                 │                            │
   │                          ├──── PATCH /chapters/{cid} ──────────────────────────────►│
   │                          │                              │                            │
   │ [✨ AI 어시스턴트] 클릭   │                              │                            │
   │ 또는 Alt+A                │                              │                            │
   ├─────────────────────────►│                              │                            │
   │                          │ Sheet 우측 슬라이드 인         │                            │
   │                          ├─────────────────────────────►│                            │
   │                          │                              │ 호출 컨텍스트 자동 세팅       │
   │                          │                              │ (현재 회차 체크 ON)           │
   │                          │                              │                            │
   │ 프리셋 선택: "이어쓰기"    │                              │                            │
   ├─────────────────────────────────────────────────────────►│                            │
   │                          │                              │                            │
   │ "✨ 생성 시작" 클릭       │                              │                            │
   ├─────────────────────────────────────────────────────────►│                            │
   │                          │                              │ POST /ai/generate (SSE) ───►│
   │                          │                              │ ◄─────── chunk: "한" ───────│
   │                          │                              │ ◄── chunk: "소년이" ────────│
   │                          │                              │ (자동 스크롤, "STREAMING ●") │
   │                          │                              │                            │
   │                          │                              │ ◄─ chunk: "[DONE]" ────────│
   │                          │                              │ 결과 도착                   │
   │                          │                              │ 액션 버튼 3개 활성          │
   │                          │                              │                            │
   │ 3개 액션 중 선택          │                              │                            │
   ├─ 끼워넣기 ──────────────►│  본문 끝에 append              │                            │
   │  또는  선택 교체         │  (에디터 선택 범위 교체)        │                            │
   │  또는  복사              │                              │                            │
   │                          │ 1.5s 하이라이트 + 토스트       │                            │
   │                          ├──── PATCH /chapters/{cid} ──────────────────────────────►│
   │                          │                              │                            │
   │ [×] 닫기                  │ Sheet 닫힘                    │                            │
   └──────────────────────────┴──────────────────────────────┴────────────────────────────┘
```

**UX 핵심**
- 결과 도착 전 액션 버튼 **비활성**(회색, tooltip "응답 도착 후 활성화")
- 끼워넣기/선택 교체/복사 **모두 명시 클릭 필요** (P1) — 자동 삽입 일절 없음
- 끼워넣기 시 커서를 본문 끝으로 이동 + 1.5s 노란색 하이라이트(`<mark>` fade out)
- Sheet 안에서 직접 본문 편집 불가 (집중 분리) — 결과는 항상 S2로 돌아가 반영

---

### 3.2 윤문 흐름 (S2 → S6 → S2, M5)

```
[사용자]                [S2 에디터]                  [S6 윤문 Dialog]            [백엔드 / im-not-ai]
   │                       │                              │                            │
   │ [🔍 윤문 실행] 또는    │                              │                            │
   │ Alt+R                 │                              │                            │
   ├──────────────────────►│                              │                            │
   │                       │  Dialog 오픈 (full-screen)    │                            │
   │                       ├─────────────────────────────►│                            │
   │                       │                              │ 강도 자동 판정              │
   │                       │                              │ (light/standard/heavy)     │
   │                       │                              │                            │
   │                       │                              │ POST /refine ──────────────►│
   │                       │                              │ {chapter_id, force_route?} │
   │                       │                              │                            │
   │                       │                              │ ◄── 200 { run_id,          │
   │                       │                              │     route_hint,            │
   │                       │                              │     spans[], original,     │
   │                       │                              │     refined,               │
   │                       │                              │     changed_ratio,         │
   │                       │                              │     gate } ────────────────│
   │                       │                              │                            │
   │                       │                              │ jsdiff 재계산 (문자 단위)     │
   │                       │                              │ ＋ 진단 span 시각화         │
   │                       │                              │ ＋ 변경률 게이트 Progress    │
   │                       │                              │                            │
   │                       │                              │ ┌─ 변경률 27% (pass) ─┐     │
   │                       │                              │ │  ████████░░  pass   │     │
   │                       │                              │ └────────────────────┘     │
   │                       │                              │                            │
   │ 검토 (스크롤 동기)      │                              │                            │
   │ 카테고리별 필터 토글    │                              │                            │
   ├──────────────────────────────────────────────────────►│                            │
   │                       │                              │                            │
   │ 수락 또는 거절          │                              │                            │
   ├─ ✓ 수락 ─────────────►│                              │ (자동저장 유지 — m-8)      │
   │   (게이트: pass/warn)  │                              ├─ POST /refine/runs/{id}/accept
   │                       │                              │ ──────────────────────────►│
   │                       │                              │ ◄── 200 { chapter updated }│
   │                       │ ◄──── 회차 본문 교체 ─────────│                            │
   │                       │      TanStack Query            │                            │
   │                       │      invalidate(['chapter'])   │                            │
   │                       │      자동저장 디바운스 정상     │                            │
   │                       │      토스트: "윤문 수락됨"     │                            │
   │                       │                              │                            │
   ├─ ✗ 거절 ─────────────►│                              │                            │
   │                       │                              ├─ POST /refine/runs/{id}/reject
   │                       │                              │ ──────────────────────────►│
   │                       │                              │ ◄── 200 { recorded } ─────│
   │                       │ (본문 변경 없음)              │                            │
   │                       │      토스트: "거절 처리됨"     │                            │
   │                       │                              │                            │
   │                       │ Dialog 닫힘                  │                            │
   └───────────────────────┴──────────────────────────────┴────────────────────────────┘
```

**50% 초과 시 (게이트: block) — C-2**
```
[사용자]                [S2 에디터]                  [S6 윤문 Dialog]            [백엔드 / im-not-ai]
   │                       │                              │                            │
   │ [수락] 버튼은 disabled  │                              │                            │
   ├──────────────────────►│                              │                            │
   │                       │ (Alert error: "50% 초과 —    │                            │
   │                       │  자동 수락 차단")             │                            │
   │                       │                              │                            │
   │ 선택지:                 │                              │                            │
   ├─ 🔄 윤문 재실행 ─────►│                              │ POST /refine (force_route) │
   │                       │                              │ (강도 변경 가능)             │
   ├─ 🗑 폐기 ────────────►│                              │                            │
   │                       │                              ├─ POST /refine/runs/{id}/reject
   │                       │                              │ (50% 초과 거절은 기록 유지)  │
   │                       │ Dialog 닫힘                  │                            │
   └───────────────────────┴──────────────────────────────┴────────────────────────────┘
```

**변경률 게이트 분기 (FR-505 / C-2 강화)**
- `changed_ratio < 0.30` (gate: `pass`) → 정상, 수락 버튼 활성
- `0.30 ≤ changed_ratio < 0.50` (gate: `warn`) → 앰버 progress, `<Alert variant="warning">` "과윤문 가능성 — 신중 검토 권장", 수락 가능
- `changed_ratio ≥ 0.50` (gate: `block`) → 빨강 progress, `<Alert variant="error">` "50% 초과 — 자동 수락 차단 — 새로 쓰기를 권장합니다", **수락 버튼 disabled + 강제 수락 경로 완전 삭제(AlertDialog 미노출)**. 선택지는 **[윤문 재실행] / [폐기]** 두 가지뿐 — 사양 v0.3 §8.3·im-not-ai 4대 철칙 #4 준수

**윤문 모듈 4대 철칙 (NFR-401)** — Dialog 헤더 고정 노출:
1. "AI 티 다듬기" 목적, 탐지 회피 아님
2. 변경률 30/50% 게이트
3. 사용자 수락/거절 결정 (자동 X)
4. 윤문 실행 시 원문 보존 (RefineRun 기록)

---

### 3.3 캐릭터 카드 편집 흐름 (S3)

```
[사용자]                  [S3 갤러리]                   [Sheet 상세]                [백엔드]
   │                          │                              │                            │
   │ 카드 클릭                 │                              │                            │
   ├─────────────────────────►│                              │                            │
   │                          │ 우측 Sheet 슬라이드 인          │                            │
   │                          ├─────────────────────────────►│                            │
   │                          │                              │ GET /characters/{id} ──────►│
   │                          │                              │ ◄── 200 card ──────────────│
   │                          │                              │ 폼 prefilled               │
   │                          │                              │                            │
   │ 필드 편집 (이름/별칭/역할/ │                              │                            │
   │ 외형/성격/말투/배경)        │                              │                            │
   │                          │                              │                            │
   │ 관계 추가: 캐릭터B 선택 +   │                              │                            │
   │ 라벨 입력                  │                              │                            │
   ├───────────────────────────────────────────────────────►│                            │
   │                          │                              │                            │
   │ [저장] 클릭                │                              │                            │
   ├───────────────────────────────────────────────────────►│                            │
   │                          │                              │ PATCH /characters/{id} ───►│
   │                          │                              │ body: { name, aliases, ... │
   │                          │                              │        card_json: { ... }  │
   │                          │                              │        relations: [...] } │
   │                          │                              │ ◄── 200 ok ────────────────│
   │                          │ ◄── Sheet 닫기 ──────────────│                            │
   │                          │ 카드 그리드 갱신 (낙관적 업데이트)│                            │
   │                          │ 토스트: "저장됨"              │                            │
   │                          │                              │                            │
   │ [+ 캐릭터 추가] 클릭       │                              │                            │
   ├─────────────────────────►│                              │                            │
   │                          │ 빈 폼 Sheet 오픈              │                            │
   │                          ├─────────────────────────────►│                            │
   │                          │                              │                            │
   │ 이름/역할 필수 입력         │                              │                            │
   │ [저장] 클릭                │                              │                            │
   ├───────────────────────────────────────────────────────►│                            │
   │                          │                              │ POST /characters ─────────►│
   │                          │                              │ ◄── 201 { id } ────────────│
   │                          │ ◄── Sheet 닫기 ──────────────│                            │
   │                          │ 그리드에 새 카드 prepend (애니메이션 짧게)│                  │
   │                          │                              │                            │
   │ 카드 우클릭 → 삭제         │                              │                            │
   ├─────────────────────────►│                              │                            │
   │                          │ AlertDialog: "정말 삭제?"     │                            │
   │                          ├─────────────────────────────►│                            │
   │                          │                              │ DELETE /characters/{id} ──►│
   │                          │                              │ ◄── 204 ──────────────────│
   │                          │ ◄── Sheet 닫기 ──────────────│                            │
   │                          │ 그리드에서 fade-out           │                            │
   │                          │ 토스트: "삭제됨 · 5초 취소"   │                            │
   └──────────────────────────┴──────────────────────────────┴────────────────────────────┘
```

**AI 보조 (선택적)**
- Sheet 안 [✨ AI로 외형 초안] 버튼 → S5 호출, 결과는 **외형 필드에 끼워넣기/선택 교체/복사** (P1)
- Sheet 안 [✨ AI로 말투 예시] 버튼 → S5 호출, 결과는 **말투 필드에 끼워넣기/선택 교체/복사** (P1)

**낙관적 업데이트**
- 카드 저장 시 그리드 카드를 먼저 갱신, 서버 응답 실패 시 롤백 + `<Alert variant="error">` (FR-408)

---

## 4. Tailwind CSS 테마 토큰 제안

> **방침** — shadcn/ui 기본 뉴트럴 팔레트(`zinc`/`slate`)은 SaaS 느낌이 강해 소설 도구에 부적합. **저채도·세리프 톤**으로 전부 덮어쓴다(§6). 다크/라이트 모두 동일한 HSL 라이트닝(명도 차이) 구조.

### 4.1 색상 (HSL 변수 — Tailwind v3 `hsl(var(--token))`)

**라이트 모드 (`<html class="light">`)**
```css
:root,
.light {
  /* 표면 (배경) — 종이 결의 따뜻한 오프화이트 */
  --background:        36 24% 96%;   /* #f7f3ec 계열 (저채도 warm) */
  --foreground:        30 14% 14%;   /* #24201c 짙은 갈색 */

  /* 카드·패널 */
  --card:              36 22% 93%;
  --card-foreground:   30 14% 14%;
  --popover:           36 22% 91%;
  --popover-foreground:30 14% 14%;

  /* 1차 액션 — 딥블루/브론즈 (사양 §6 제안) */
  --primary:           215 32% 28%;   /* 딥블루 #2c3e5a 계열 */
  --primary-foreground:36 24% 96%;

  /* 2차 — 브론즈 */
  --secondary:         28 22% 38%;
  --secondary-foreground:36 24% 96%;

  /* 뮤트 — 본문 보조 텍스트·구분선 */
  --muted:             36 14% 86%;
  --muted-foreground:  30 8% 42%;

  /* 강조 — 보존/저장 표시 */
  --accent:            28 28% 46%;
  --accent-foreground: 36 24% 96%;

  /* 위험 */
  --destructive:       4 42% 42%;    /* 저채도 brick red */
  --destructive-foreground:36 24% 96%;

  /* 경고 (윤문 30% 게이트) */
  --warning:           30 50% 42%;   /* 앰버 저채도 */
  --warning-foreground:30 14% 14%;

  /* 정보 (AI 패널 안내) */
  --info:              200 24% 38%;
  --info-foreground:   36 24% 96%;

  /* 보더·인풋 */
  --border:            30 12% 78%;
  --input:             30 12% 80%;
  --ring:              215 32% 38%;

  /* 상태 칩 (FR-105) */
  --status-draft:      30 8% 56%;    /* 초고 — 회색 */
  --status-revising:   30 50% 46%;   /* 수정중 — 앰버 저채도 */
  --status-done:       150 28% 36%;  /* 완료 — 에메랄드 저채도 */

  /* 윤문 카테고리 10종 (FR-503 / M-4 taxonomy ID A~J) — 각 HSL 1쌍, 라이트 배경 위 가독성 */
  --cat-A:  4 50% 42%;   /* A 번역투 */
  --cat-B:  220 30% 38%; /* B 영어인용 */
  --cat-C:  280 24% 38%; /* C 구조적패턴 (기계적 병렬) */
  --cat-D:  160 28% 32%; /* D AI 특유 관용구 */
  --cat-E:  340 26% 40%; /* E 리듬·문장 길이 균일성 */
  --cat-F:  30 36% 36%;  /* F 과도한 수식·중복 */
  --cat-G:  200 28% 36%; /* G Hedging */
  --cat-H:  12 36% 40%;  /* H 접속사 남발 */
  --cat-I:  48 36% 36%;  /* I 형식명사 과다 */
  --cat-J:  0 0% 38%;    /* J 시각 장식(이모지·구분선) 남용 */

  /* diff 색 (S6) */
  --diff-add:          150 32% 38%;
  --diff-del:          4 50% 42%;
  --diff-add-bg:       150 32% 92%;
  --diff-del-bg:       4 50% 92%;
}
```

**다크 모드 (`<html class="dark">`)**
```css
.dark {
  --background:        30 8% 12%;    /* #1d1b18 짙은 갈색 */
  --foreground:        36 18% 88%;   /* #e3ddd2 */

  --card:              30 8% 15%;
  --card-foreground:   36 18% 88%;
  --popover:           30 8% 18%;
  --popover-foreground:36 18% 88%;

  --primary:           215 38% 64%;   /* 라이트 딥블루 → 다크 라이트블루 */
  --primary-foreground:30 8% 12%;

  --secondary:         28 28% 56%;
  --secondary-foreground:30 8% 12%;

  --muted:             30 6% 22%;
  --muted-foreground:  36 10% 64%;

  --accent:            28 32% 58%;
  --accent-foreground: 30 8% 12%;

  --destructive:       4 50% 56%;
  --destructive-foreground:30 8% 12%;

  --warning:           30 56% 58%;
  --warning-foreground:30 8% 12%;

  --info:              200 36% 60%;
  --info-foreground:   30 8% 12%;

  --border:            30 6% 26%;
  --input:             30 6% 28%;
  --ring:              215 38% 64%;

  --status-draft:      30 8% 56%;
  --status-revising:   30 56% 58%;
  --status-done:       150 32% 52%;

  /* 윤문 카테고리는 다크 톤에 맞춰 lightness ↑ (taxonomy ID A~J 매핑은 라이트와 동일 순서) */
  --cat-A:  4 56% 64%;   /* A 번역투 */
  --cat-B:  220 44% 68%; /* B 영어인용 */
  --cat-C:  280 38% 70%; /* C 구조적패턴 */
  --cat-D:  160 36% 58%; /* D 관용구 */
  --cat-E:  340 38% 68%; /* E 리듬 */
  --cat-F:  30 48% 62%;  /* F 수식중복 */
  --cat-G:  200 40% 64%; /* G Hedging */
  --cat-H:  12 50% 64%;  /* H 접속사 */
  --cat-I:  48 50% 60%;  /* I 형식명사 */
  --cat-J:  0 0% 64%;    /* J 시각장식 */

  --diff-add:          150 40% 60%;
  --diff-del:          4 56% 64%;
  --diff-add-bg:       150 32% 18%;
  --diff-del-bg:       4 40% 18%;
}
```

**`tailwind.config.ts` 토큰 매핑**
```ts
extend: {
  colors: {
    border: 'hsl(var(--border))',
    input: 'hsl(var(--input))',
    ring: 'hsl(var(--ring))',
    background: 'hsl(var(--background))',
    foreground: 'hsl(var(--foreground))',
    primary: { DEFAULT: 'hsl(var(--primary))', foreground: 'hsl(var(--primary-foreground))' },
    secondary: { DEFAULT: 'hsl(var(--secondary))', foreground: 'hsl(var(--secondary-foreground))' },
    destructive: { DEFAULT: 'hsl(var(--destructive))', foreground: 'hsl(var(--destructive-foreground))' },
    muted: { DEFAULT: 'hsl(var(--muted))', foreground: 'hsl(var(--muted-foreground))' },
    accent: { DEFAULT: 'hsl(var(--accent))', foreground: 'hsl(var(--accent-foreground))' },
    popover: { DEFAULT: 'hsl(var(--popover))', foreground: 'hsl(var(--popover-foreground))' },
    card: { DEFAULT: 'hsl(var(--card))', foreground: 'hsl(var(--card-foreground))' },
    warning: { DEFAULT: 'hsl(var(--warning))', foreground: 'hsl(var(--warning-foreground))' },
    info: { DEFAULT: 'hsl(var(--info))', foreground: 'hsl(var(--info-foreground))' },
    status: {
      draft: 'hsl(var(--status-draft))',
      revising: 'hsl(var(--status-revising))',
      done: 'hsl(var(--status-done))',
    },
    cat: {
      // M-4: taxonomy ID(A~J) — 백엔드 §5 span.category와 1:1 대응
      // 사용 예: <span className="bg-cat-A/15 text-cat-A">[번역투]</span>
      A: 'hsl(var(--cat-A))', B: 'hsl(var(--cat-B))',
      C: 'hsl(var(--cat-C))', D: 'hsl(var(--cat-D))',
      E: 'hsl(var(--cat-E))', F: 'hsl(var(--cat-F))',
      G: 'hsl(var(--cat-G))', H: 'hsl(var(--cat-H))',
      I: 'hsl(var(--cat-I))', J: 'hsl(var(--cat-J))',
    },
    diff: {
      add: 'hsl(var(--diff-add))', del: 'hsl(var(--diff-del))',
      addBg: 'hsl(var(--diff-add-bg))', delBg: 'hsl(var(--diff-del-bg))',
    },
  },
}
```

### 4.2 타이포그래피 — 본문 세리프 (사양 §6 "국산 세리프/고딕 가독형")

```ts
fontFamily: {
  // 본문 — 한글 웹소설 원고용 세리프
  serif: [
    '"Noto Serif KR"',      // 1순위: 구글 본산 세리프, 모든 weight
    '"Source Serif Pro"',   // 라틴 본문
    '"Pretendard"',         // 폴백 세리프(없으면 고딕)
    'Georgia', 'serif',
  ],
  // UI(메뉴, 버튼, 칩) — 고딕 가독형
  sans: [
    '"Pretendard"',         // 1순위 한글 UI
    '"Inter"', '"system-ui"',
    'sans-serif',
  ],
  // 코드/미리보기/글자수 — 모노스페이스
  mono: [
    '"JetBrains Mono"', '"Fira Code"', '"D2Coding"',
    'ui-monospace', 'monospace',
  ],
},
fontSize: {
  // 원고 본문은 넉넉히
  'prose': ['1.0625rem', { lineHeight: '1.85', letterSpacing: '0.005em' }],
  'prose-lg': ['1.1875rem', { lineHeight: '1.9', letterSpacing: '0.01em' }],
  'prose-sm': ['0.9375rem', { lineHeight: '1.7' }],
},
```

**에디터 적용 (CodeMirror 6)**
- `EditorView.theme({ '&': { fontFamily: 'var(--font-serif)', fontSize: '1.0625rem', lineHeight: '1.85' } })`
- 가독성: 어두운 다크 모드에서 `--foreground` (`#e3ddd2`) 대비 — WCAG AA 7:1+

**미리보기(markdown-it)**
- 본문 영역 `font-serif text-prose`, 제목은 `font-sans font-semibold` (UI 톤)
- `prose-headings:font-sans`, `prose-p:font-serif`

**글자 수 푸터 (FR-104)**
- `<span className="font-mono tabular-nums">` — 숫자 정렬 안정

### 4.3 간격·반경·그림자 (저채도 톤)

```ts
borderRadius: { lg: '0.5rem', md: '0.375rem', sm: '0.25rem' },
boxShadow: {
  soft: '0 1px 2px hsl(var(--foreground) / 0.04), 0 4px 12px hsl(var(--foreground) / 0.06)',
  focus: '0 0 0 3px hsl(var(--ring) / 0.3)',
},
transitionDuration: { DEFAULT: '120ms', fast: '80ms' },
```

> **금지**: 화려한 그림자, 그라데이션, 글로우 — 소설 도구의 차분한 톤 유지.

---

## 5. Zustand 스토어 경계 설계 (사양 §6 "에디터/AI패널/설정")

> **원칙** (사양 §6 · §2.2): 컴포넌트 시안은 `frontend/src/components/{ui,editor,panels,features}` 구조, Zustand 스토어 경계(editor/aiPanel/settings)를 설계에 명시.

### 5.1 스토어 4종 (서버 상태는 TanStack Query로 분리)

```
src/stores/
├── editorStore.ts       ← 에디터 로컬 UI 상태 (사양 §6)
├── aiPanelStore.ts      ← AI 패널 UI + 호출 컨텍스트 (사양 §6)
├── settingsStore.ts     ← 사용자 환경설정 (사양 §6)
└── uiStore.ts           ← 전역 UI (테마, RightPanel 열림/닫힘, 단축키 모달)
```

> 서버 데이터(프로젝트/회차/캐릭터/로어북/엔드포인트/윤문 리포트)는 **TanStack Query**가 담당 — Zustand는 순수 **클라이언트 UI 상태**만. 경계 명확화.

### 5.2 `editorStore`

```ts
interface EditorState {
  // 현재 컨텍스트
  projectId: string | null;
  chapterId: string | null;

  // 에디터 (CodeMirror 6 핸들 — View 인스턴스 참조, 콘텐츠는 서버 진실)
  view: EditorView | null;
  setView: (v: EditorView | null) => void;

  // 자동 저장
  isSaving: boolean;
  lastSavedAt: number | null;       // Date.now()
  nextSaveAt: number | null;       // 디바운스 종료 시각
  setSaving: (b: boolean) => void;
  markSaved: () => void;

  // 모드
  mode: 'edit' | 'preview';
  setMode: (m: 'edit' | 'preview') => void;

  // 회차 트리
  expandedVolumes: Set<string>;
  toggleVolume: (vid: string) => void;

  // 글자 수 (캐시)
  wordCount: { total: number; noSpace: number };
  setWordCount: (w: { total: number; noSpace: number }) => void;
}
```

**책임**: 에디터 마운트/언마운트, 자동저장 인디케이터, 모드 토글, 회차 트리 펼침 상태, 글자 수.
**금지**: 본문 텍스트 자체는 저장 X (서버 진실 = TanStack Query 캐시).

### 5.3 `aiPanelStore`

```ts
interface AiPanelState {
  // 열림/닫힘
  isOpen: boolean;
  open: () => void;
  close: () => void;
  toggle: () => void;

  // 모드 (S5 단독)
  mode: 'ai' | 'refine';           // 단일 Sheet 인스턴스, 모드 스위치
  setMode: (m: 'ai' | 'refine') => void;

  // 호출 컨텍스트 (Sheet에 들어올 때 자동 세팅)
  source: { page: 'editor' | 'character' | 'lore'; entityId?: string };
  contextSelection: {
    chapterId: string | null;
    characterIds: string[];
    loreIds: string[];
  };
  setContext: (c: Partial<AiPanelState['contextSelection']>) => void;

  // 호출 폼
  endpointId: string | null;
  presetId: string | null;
  promptOverride: string;
  temperature: number;
  maxTokens: number;
  setEndpoint: (id: string) => void;
  setPreset: (id: string) => void;
  setPrompt: (s: string) => void;
  setParams: (p: Partial<Pick<AiPanelState, 'temperature' | 'maxTokens'>>) => void;

  // 스트리밍 (실시간 토큰은 in-memory ref로 두고 Zustand에는 상태만)
  status: 'idle' | 'streaming' | 'done' | 'error';
  streamingText: string;             // 누적 결과
  error: { code: string; message: string } | null;
  startStream: () => void;
  appendChunk: (s: string) => void;
  finishStream: () => void;
  failStream: (e: AiPanelState['error']) => void;
  reset: () => void;
}
```

**책임**: Sheet 가시성·모드, 컨텍스트, 폼, 스트리밍 상태.
**금지**: 토큰 텍스트 영구 저장 X (메모리). SSE 종료 시 `resultText`는 호출 직후 액션 버튼에서만 사용 후 폐기.

### 5.4 `settingsStore` (사용자 환경설정 — DB 영속)

```ts
interface SettingsState {
  theme: 'light' | 'dark' | 'system';
  setTheme: (t: 'light' | 'dark' | 'system') => void;

  fontFamily: 'pretendard' | 'noto-serif-kr';
  lineHeight: 1.7 | 1.8 | 1.9;
  letterSpacing: 'normal' | 'wide';
  setTypography: (t: Partial<Pick<SettingsState, 'fontFamily' | 'lineHeight' | 'letterSpacing'>>) => void;

  autoSaveIntervalMs: number;        // 500~5000, 기본 1500
  setAutoSaveInterval: (ms: number) => void;

  defaultRefineRoute: 'auto' | 'light' | 'standard' | 'heavy';
  setDefaultRefineRoute: (r: SettingsState['defaultRefineRoute']) => void;

  // hydrate (앱 시작 시 DB에서 로드)
  hydrate: (s: Partial<SettingsState>) => void;
}
```

**책임**: 사용자가 S7에서 고른 환경설정. 영속은 백엔드 `GET/PATCH /api/v1/settings`.

### 5.5 `uiStore` (휘발성 UI)

```ts
interface UiState {
  // RightPanel 단축키
  rightPanelTab: 'ai' | 'refine' | null;
  setRightPanelTab: (t: UiState['rightPanelTab']) => void;

  // 단축키 모달
  shortcutsOpen: boolean;
  setShortcutsOpen: (b: boolean) => void;

  // 토스트 큐 (sonner 래퍼)
  pushToast: (t: { title: string; variant?: 'info' | 'success' | 'warning' | 'error' }) => void;

  // 명령 팔레트 (Ctrl+K) — 백로그
  commandOpen: boolean;
}
```

### 5.6 스토어 경계 다이어그램

```
┌──────────────────┐    ┌──────────────────┐
│   editorStore    │    │   aiPanelStore   │
│  (에디터 로컬)    │    │ (AI/윤문 Sheet)  │
│                  │    │                  │
│  - chapterId     │◄──►│  - contextSel.   │   ← 호출 컨텍스트 공유
│  - view (CM6)    │    │    chapterId     │
│  - isSaving      │    │  - streaming     │
│  - wordCount     │    │                  │
└──────────────────┘    └──────────────────┘
         │                       │
         ▼                       ▼
   ┌─────────────────────────────────────┐
   │          TanStack Query             │
   │  (서버 진실 — 프로젝트/회차/캐릭터/    │
   │   로어북/엔드포인트/윤문리포트)        │
   └─────────────────────────────────────┘
         ▲                       ▲
         │                       │
┌──────────────────┐    ┌──────────────────┐
│  settingsStore   │    │     uiStore      │
│  (영속 설정)      │    │   (휘발성 UI)    │
└──────────────────┘    └──────────────────┘
```

**상호작용 규칙**
- `editorStore.chapterId` 변경 → `aiPanelStore.setContext({ chapterId })` 자동 (S5 오픈 트리거에서)
- `aiPanelStore.action=accept` → `editorStore` 의 `view` 에 PATCH dispatch + TanStack Query invalidate
- 설정 변경 → 즉시 적용 (Tailwind 토큰은 `:root` class 토글로 즉시 반영)

---

## 6. 키보드 단축키 & 접근성 체크리스트

### 6.1 단축키 (전역 우선, 충돌 방지)

| 단축키 | 동작 | 화면 | 비고 |
|--------|------|------|------|
| `Ctrl/Cmd + S` | 수동 저장 (debounce 우회 즉시 PATCH) | S2·S3·S4 | FR-106 |
| `Ctrl/Cmd + N` | 새 (프로젝트/회차/캐릭터/로어북 — 현재 페이지 컨텍스트) | S1~S4 | |
| `Ctrl/Cmd + K` | 명령 팔레트 (백로그) | 전역 | |
| `Alt + A` | AI 패널 토글 (열기/닫기) | S2·S3·S4 | NFR-303 |
| `Alt + R` | 윤문 실행 (현재 회차) | S2 | |
| `Alt + ←/→` | 회차 이전/다음 | S2 | |
| `Alt + 1~4` | LeftSidebar 카테고리 전환 (프로젝트/권/캐릭터/로어북) | S2~S4 | |
| `Ctrl/Cmd + B` | 굵게 (마크다운) | S2 | CM6 keymap |
| `Ctrl/Cmd + I` | 기울임 | S2 | CM6 keymap |
| `Ctrl/Cmd + D` | 줄 삭제 | S2 | CM6 keymap |
| `Ctrl/Cmd + F` | 에디터 내 검색 | S2 | CM6 `@codemirror/search` |
| `Ctrl/Cmd + H` | 에디터 내 검색·치환 | S2 | CM6 |
| `Ctrl/Cmd + .` | 설정(S7)으로 이동 | 전역 | |
| `Ctrl/Cmd + /` | 단축키 도움말 토글 | 전역 | NFR-301 |
| `Esc` | Sheet/Dialog 닫기 | S3·S4·S5·S6·S7 | |
| `Tab` / `Shift+Tab` | 포커스 순환 (Radix 자동) | 전역 | a11y |

**CodeMirror 6 keymap 확장**
```ts
import { keymap } from '@codemirror/view';
keymap.of([
  { key: 'Mod-s', run: () => { saveNow(); return true; } },
  { key: 'Alt-a', run: () => { aiPanelStore.toggle(); return true; } },
  { key: 'Alt-r', run: () => { openRefine(); return true; } },
  { key: 'Alt-ArrowLeft', run: () => { gotoPrevChapter(); return true; } },
  { key: 'Alt-ArrowRight', run: () => { gotoNextChapter(); return true; } },
]),
```

### 6.2 접근성 체크리스트 (WCAG 2.1 AA 목표, Radix 기반)

**인식 (Perceivable)**
- [ ] 모든 텍스트/배경 명도 대비 ≥ 4.5:1 (일반), ≥ 3:1 (큰 텍스트 ≥18pt) — 토큰 위주 검증
- [ ] 색만으로 정보 전달 금지 — 상태 칩은 색 + 텍스트(초고/수정중/완료)
- [ ] AI 결과 Alert는 색 + 아이콘 + 텍스트 3중 (`ShieldCheck` + "AI 결과는 자동 삽입되지 않습니다")
- [ ] 윤문 변경률 게이트는 색 + 라벨(0~30% / 30~50% / 50%+) + Progress 텍스트
- [ ] 아이콘 버튼은 `aria-label` 필수 (`lucide-react` 아이콘 단독 사용 X)
- [ ] 다크/라이트 모두 명도 대비 검증 (Axe 자동화)

**운용 (Operable)**
- [ ] 모든 인터랙티브 요소 키보드 접근 (Tab 순서 논리적)
- [ ] Sheet/Dialog는 포커스 트랩(Radix 기본), `Esc`로 닫힘, 닫힐 때 트리거로 포커스 복귀
- [ ] 모달 열린 동안 배경 스크롤 방지
- [ ] 단축키는 마우스 없이 모든 핵심 작업 가능 (`Ctrl+S`, `Alt+A`, `Alt+R`, `Alt+←/→`)
- [ ] 드래그(회차 트리 정렬)는 마우스 전용 시 키보드 대안 제공 (백로그 — dnd-kit + 키보드 센서)

**이해 (Understandable)**
- [ ] 한국어 UI 전원 (NFR-302) — 영문 라벨 최소화
- [ ] 입력 검증 에러는 즉시 + 한국어 + 수정 제안 (RHF + zod)
- [ ] 빈 상태에 다음 행동 CTA 1개 ("+ 새 작품" 등)

**견고 (Robust)**
- [ ] Radix UI 기반 → ARIA 속성 자동 (`role`, `aria-expanded`, `aria-controls`)
- [ ] shadcn/ui Dialog/Sheet/Tabs/Menu 모두 ARIA 패턴 준수
- [ ] CodeMirror 6 — `aria-label="원고 에디터"`, `aria-describedby`로 글자수 영역 연결
- [ ] 토스트(`sonner`)는 `role="status"` + `aria-live="polite"`, 에러는 `assertive`

**추가 — 운동 보조 / 인지 보조**
- [ ] 모든 클릭 타겟 ≥ 44×44px (Tailwind `min-h-11 min-w-11`)
- [ ] 자동저장·저장 상태는 `prefers-reduced-motion` 사용자에게 애니메이션 단축
- [ ] 윤문 diff에서 카테고리 색만 보던 사용자도 텍스트 라벨로 구분 가능 (호버 툴팁 + 키보드 탐색)

**검증 도구**
- `axe-core` (Vitest + jsdom), `@axe-core/playwright` (E2E)
- 키보드만으로 S1~S7 전 흐름 1회 수행 — 수동 체크리스트
- 스크린리더(NVDA) — AI 패널 호출·윤문 리포트 수락 플로우 1회

---

### 6.3 시니어 배려 모드 (v1.3 신설 — 접근성 보강)

> 대상 사용자에 장시간 글을 쓰는 중장년·시니어 작가가 포함될 수 있으므로, S7 테마 탭(S-703)에서 켤 수 있는 **[시니어 배려 모드] 토글 1개**로 아래 설정을 일괄 적용한다. 개별 항목은 기존 설정으로도 각각 조정 가능하다(모드는 프리셋 성격).

| 항목 | 시니어 배려 모드 적용값 | 기본값 | 구현 |
|------|------------------------|--------|------|
| UI 배율 | 112.5%(`html { font-size: 18px }`) — 라벨·버튼·본문 전체 확대 | 100% | `settingsStore.uiScale`, `:root` class 토글 |
| 본문 크기/행간 | 에디터 `prose-lg`(1.1875rem) + 행간 1.9 고정 제안 | prose 1.85 | settingsStore.typography |
| 클릭 타겟 | 최소 48×48px로 상향(WCAG 2.5.5 AAA 권고 수준) | 44×44px | Tailwind 유틸 `min-h-12 min-w-12` 스왑 |
| 명도 대비 | 본문/보조 텍스트 대비 ≥ 7:1로 강화(고대비 토큰 세트 적용 — `--muted-foreground` lightness ↓, 다크 모드 ↑) | AA 4.5:1 | `data-contrast="high"` 토큰 오버라이드 |
| 애니메이션 | 전환·하이라이트 깜빡임 축소(`prefers-reduced-motion`과 독립 강제) | 120ms | transitionDuration fast=0, flash 데코 생략 |
| 언어 | 모든 라벨·오류를 짧은 한국어 문장 유지, 전문 용어(route_hint 등)에는 한국어 설명 툴팁 부착 | — | F-035 한국어 UI 원칙 강화 |

**키보드 조작 보강(전 화면 공통)**:
- 포커스 링(`--ring`)은 시니어 배려 모드에서 두께 3px·대비 강화로 표시 — 키보드 위치 인지성 향상
- Sheet/Dialog 내 Tab 순환은 Radix 기본 트랩 유지 + 첫 포커스를 제목 요소에 배치(`aria-labelledby` 연결)
- 회차 트리(S-006)는 방향키 탐색 지원(↑/↓ 이동, → 펼치기, ← 접기, Enter 열기) — 드래그 정렬의 키보드 대안

**한국어 라벨 점검 결과(v1.3)**: 전 화면 라벨·버튼·오류 메시지 한국어 확인 완료(F-035). 영문 잔류는 기술 식별자(`base_url`, `max_tokens`, `temperature` 등 입력 필드 도움말)뿐이며, 각 필드에 한국어 `<FormDescription>`을 병기한다.

**검증**: axe-core 규칙에 시니어 배려 모드 ON/OFF 2회 스캔을 추가하고, ON 상태에서 대비 위반 0건을 G7 QA 체크리스트에 포함한다.

---

## 7. 개발팀 구현 노트

### 7.1 컴포넌트 우선순위 (1차 스프린트)

| 순위 | 컴포넌트 | 의존 | 비고 |
|:----:|----------|------|------|
| **P0-A** | `ThemeProvider` + 토큰 (`globals.css`, `tailwind.config.ts`) | — | 모든 화면 토대 |
| **P0-B** | `App` 셸 + 라우팅 (react-router) + 3분할 레이아웃 + `TopBar`/`StatusBar` | shadcn 설치 | §1.3 컴포넌트 트리 |
| **P0-C** | `editorStore` + CodeMirror 6 래퍼 `<Editor>` + 글자 수 표시 | `@codemirror/view` `lang-markdown` | S2 핵심, NFR-101 |
| **P1-A** | 회차 트리 `<ChapterTree>` (Radix Collapsible) + 회차 CRUD | `editorStore` | S2 좌측 |
| **P1-B** | `aiPanelStore` + `<AiPanel>` Sheet + SSE 스트리밍 | `fetch` 스트림(SSE) + `lib/aiStream.ts`, 백엔드 `/ai/generate` | S5, FR-405 |
| **P1-C** | 자동 저장 (디바운스 + PATCH) + 저장 상태 표시 | `editorStore` | FR-106, NFR-204 |
| **P2-A** | S1 홈 — 프로젝트 카드 그리드 + 생성 Dialog | TanStack Query | FR-101 |
| **P2-B** | S3 캐릭터 갤러리 + Sheet 상세 | TanStack Query | FR-201~205 |
| **P2-C** | S4 로어북 + Sheet 상세 | TanStack Query | FR-301~305 |
| **P3-A** | `<RefineReport>` Dialog + jsdiff 병렬 뷰 + 변경률 게이트 | `jsdiff` | S6, FR-503~505 |
| **P3-B** | S7 설정 — AI 엔드포인트/윤문/테마/규정/백업 | TanStack Query | FR-401~407, NFR-203 |
| **P4** | 미리보기 탭 (markdown-it + DOMPurify) | `markdown-it` `dompurify` | FR-103 |
| **P4** | 단축키 도움말 모달 | `uiStore` | NFR-301 |
| **P5** | 음성 입력(Web Speech API), 접근성 강화 | 백로그 | — |

### 7.2 CodeMirror 6 연동 포인트

**패키지 (확정)**
```
@codemirror/view         6.x
@codemirror/state        6.x
@codemirror/commands     6.x
@codemirror/lang-markdown
@codemirror/language     6.x
@codemirror/search       6.x
@codemirror/autocomplete 6.x
@codemirror/theme-one-dark  (참고만, 직접 토큰화)
```

**래퍼 구조** (`src/components/editor/Editor.tsx`)
```
<Editor
  value={chapter.content_md}            // 외부 진실 (TanStack Query)
  onChange={(text) => debouncedPatch(text)}
  theme={settingsStore.theme}
  font={settingsStore.fontFamily}
  lineHeight={settingsStore.lineHeight}
  readOnly={refineStore.isOpen}
  ariaLabel="원고 에디터"
/>
```

**핵심 통합 코드 스케치**
```tsx
import { EditorState, Compartment } from '@codemirror/state';
import { EditorView, keymap, lineNumbers, highlightActiveLine } from '@codemirror/view';
import { defaultKeymap, history, historyKeymap } from '@codemirror/commands';
import { markdown } from '@codemirror/lang-markdown';
import { searchKeymap, highlightSelectionMatches } from '@codemirror/search';

const themeCompartment = new Compartment();
const readOnlyCompartment = new Compartment();

function buildState({ value, theme, font, lineHeight, readOnly, onSave, onAiToggle, onRefineOpen }: Props) {
  return EditorState.create({
    doc: value,
    extensions: [
      // m-7: 기본 OFF(집중 모드) + settingsStore.editor.lineNumbers 토글로 ON — Compartment로 런타임 전환
      settings.editor.lineNumbers ? lineNumbers() : [],  // 기본 OFF (집중 모드)
      history(),
      highlightActiveLine(),
      highlightSelectionMatches(),
      markdown(),
      EditorView.lineWrapping,
      keymap.of([
        ...defaultKeymap,
        ...historyKeymap,
        ...searchKeymap,
        { key: 'Mod-s', run: () => { onSave(); return true; } },
        { key: 'Alt-a', run: () => { onAiToggle(); return true; } },
        { key: 'Alt-r', run: () => { onRefineOpen(); return true; } },
      ]),
      themeCompartment.of(EditorView.theme({
        '&': { fontFamily: 'var(--font-serif)', fontSize: '1.0625rem', lineHeight },
        '.cm-content': { caretColor: 'hsl(var(--primary))' },
        '.cm-gutters': { background: 'transparent', borderRight: '1px solid hsl(var(--border))' },
        '&.cm-focused .cm-cursor': { borderLeftColor: 'hsl(var(--primary))' },
        '.cm-selectionBackground': { background: 'hsl(var(--primary) / 0.18) !important' },
      })),
      readOnlyCompartment.of(EditorState.readOnly.of(readOnly)),
      EditorView.updateListener.of((u) => {
        if (u.docChanged) onChange(u.state.doc.toString());
      }),
    ],
  });
}
```

**글자 수 계산** (`src/lib/wordCount.ts`)
```ts
export function count(text: string) {
  const total = text.length;
  const noSpace = text.replace(/\s/g, '').length;
  return { total, noSpace };
}
// CodeMirror의 updateListener에서 매 변경마다 호출,
// editorStore.setWordCount → 푸터 표시 (FR-104)
```

**자동 저장 디바운스 + PATCH**
```ts
function useAutoSave(chapterId: string) {
  const queryClient = useQueryClient();
  const { isSaving, markSaved, setSaving } = useEditorStore();
  const debounced = useDebouncedCallback(async (text: string) => {
    setSaving(true);
    try {
      await api.patch(`/chapters/${chapterId}`, { content_md: text });
      markSaved();
      queryClient.invalidateQueries(['chapter', chapterId]);
    } catch (e) {
      toast.error('저장 실패 — 재시도 중');
    } finally {
      setSaving(false);
    }
  }, 1500);
  return debounced;
}
```

**lineNumbers 런타임 토글 (m-7) — `src/components/editor/Editor.tsx`**
```ts
// 설정 토글 시 Compartment.reconfigure로 즉시 전환 (에디터 인스턴스 재생성 X)
const lineNumbersCompartment = new Compartment();

function toggleLineNumbers(view: EditorView, on: boolean) {
  view.dispatch({
    effects: lineNumbersCompartment.reconfigure(on ? lineNumbers() : []),
  });
}

// S7 설정 > 고급 탭의 "에디터 줄 번호" Switch → settingsStore.editor.lineNumbers
// 변경 감지 후 toggleLineNumbers(view, settings.editor.lineNumbers) 호출
```

**윤문 결과 끼워넣기 (S5 → S2)**
```ts
function insertOrReplace(view: EditorView, text: string, mode: 'append' | 'replace') {
  const changes = mode === 'append'
    ? { from: view.state.doc.length, to: view.state.doc.length, insert: '\n' + text }
    : view.state.selection.ranges.length > 0 && !view.state.selection.main.empty
      ? { from: view.state.selection.main.from, to: view.state.selection.main.to, insert: text }
      : { from: view.state.doc.length, to: view.state.doc.length, insert: '\n' + text };
  view.dispatch({ changes, selection: { anchor: view.state.doc.length } });
  view.focus();
  // 1.5s 하이라이트
  const deco = Decoration.mark({ class: 'cm-inserted-flash' });
  // ... setTimeout으로 deco 제거
}
```

### 7.3 SSE 스트리밍 클라이언트 (FR-405)

```ts
// src/lib/aiStream.ts
export function streamGenerate(body: GenerateBody, onChunk: (s: string) => void, onDone: () => void, onError: (e: Error) => void) {
  const url = `/api/v1/ai/generate`;
  const ctrl = new AbortController();
  fetch(url, {
    method: 'POST',
    headers: { 'content-type': 'application/json', accept: 'text/event-stream' },
    body: JSON.stringify(body),
    signal: ctrl.signal,
  }).then(async (res) => {
    if (!res.ok || !res.body) { onError(new Error(`HTTP ${res.status}`)); return; }
    const reader = res.body.getReader();
    const dec = new TextDecoder();
    let buf = '';
    while (true) {
      const { value, done } = await reader.read();
      if (done) break;
      buf += dec.decode(value, { stream: true });
      const events = buf.split('\n\n');
      buf = events.pop() ?? '';
      for (const ev of events) {
        const line = ev.split('\n').find(l => l.startsWith('data:'));
        if (!line) continue;
        const data = line.slice(5).trim();
        if (data === '[DONE]') { onDone(); return; }
        try { onChunk(JSON.parse(data).text); } catch {}
      }
    }
  }).catch(onError);
  return () => ctrl.abort();
}
```

> **중단**: S5의 [⏹ 중단] 버튼이 `ctrl.abort()` 호출 → 백엔드는 sse-starlette 응답 종료. NFR-102(스트리밍 중 UI 멈춤 금지)는 fetch를 메인 스레드 비동기로 처리하므로 충족.

### 7.4 shadcn/ui 컴포넌트 설치 목록 (개발팀 1단계)

```bash
npx shadcn@latest init            # Tailwind + tokens 부트스트랩
npx shadcn@latest add button input textarea select slider \
  switch checkbox radio-group tabs dialog sheet drawer \
  popover tooltip dropdown-menu alert card badge skeleton \
  form label separator scroll-area progress collapsible \
  sonner command
# + react-router-dom, zustand, @tanstack/react-query
# + @codemirror/{view,state,commands,lang-markdown,search,language}
# + markdown-it dompurify
# + diff (jsdiff)
# + lucide-react
```

### 7.5 파일 구조 체크 (사양 §2.2 준수)

```
frontend/src/
├── components/
│   ├── ui/         ← shadcn 생성 (Button, Dialog, Sheet, Tabs, ...)
│   ├── editor/     ← Editor, EditorPreview, WordCountFooter, SaveIndicator
│   ├── panels/     ← AiPanel, RefineReport, DiffView, ChangeRatioGate
│   └── features/   ← ChapterTree, ProjectCardGrid, CharacterCard, LoreEntryCard
├── pages/          ← Home, Editor, CharacterGallery, Lorebook, Settings
├── stores/         ← editorStore, aiPanelStore, settingsStore, uiStore
├── lib/            ← api.ts, aiStream.ts, wordCount.ts, diff.ts, cn.ts
├── styles/         ← globals.css (토큰), tailwind.config.ts
└── App.tsx         ← 라우팅 + 3분할 셸
```

### 7.6 AI 패널 호출 진입점 구현 노트

| 호출 위치 | 구현 |
|----------|------|
| S2 헤더 [✨ AI 어시스턴트] | `useAiPanel().open('editor', { chapterId: editorStore.chapterId })` |
| S2 FAB [▶ AI 패널] | 동일 |
| S3 Sheet [✨ AI로 외형 초안] | `open('character', { entityId: character.id, targetField: 'appearance' })` |
| S3 Sheet [✨ AI로 말투 예시] | `open('character', { entityId: character.id, targetField: 'speech_style' })` |
| S4 Sheet [✨ AI로 본문 다듬기] | `open('lore', { entityId: lore.id, targetField: 'content' })` |
| 전역 `Alt+A` | 마지막 컨텍스트로 `toggle()` |

**호출 시 자동 세팅** (S5 Sheet 마운트 시):
- `mode = 'ai'`
- `contextSelection.chapterId = 현재 회차` (S2에서 호출 시)
- `contextSelection.characterIds = [현재 캐릭터]` (S3에서 호출 시)
- `contextSelection.loreIds = [현재 항목]` (S4에서 호출 시)
- 체크박스는 모두 ON, 사용자가 직접 토글

### 7.7 윤문 모듈 호출 진입점

| 호출 | 구현 |
|------|------|
| S2 [🔍 윤문 실행] | `useRefine().open({ chapterId })` → `aiPanelStore.setMode('refine')` |
| `Alt+R` | 동일 (에디터 focus 상태에서) |
| S6 [🔄 다시 실행] | 강도 선택 후 `POST /refine` 재실행 |
| S6 [✓ 수락] | `POST /refine/runs/{id}/accept` → 본문 교체(**자동저장 유지**, OFF/ON 윈도우 불필요) → `queryClient.invalidateQueries(['chapter', cid])` + `['refine-runs', cid]` |
| S6 [✗ 거절] | `POST /refine/runs/{id}/reject` → Dialog 닫기 |

### 7.8 마이그레이션 / 호환성

- **CodeMirror 6 아카이브 대응** (사양 §8.5): npm `@codemirror/*` 6.43.9까지 안정, 분기별 `npm outdated` 점검. 자체 래퍼를 얇게 유지해 교체 비용 최소화.
- **shadcn/ui**: 컴포넌트 코드가 프로젝트 내 `components/ui`에 복사되므로 라이선스/공급망 리스크 없음. 토큰은 §4로 전부 덮어씀.
- **jsdiff**: BSD-3-Clause, FR-504·505 충족. `diffChars` + 라인 정렬은 커스텀.

### 7.9 검증 게이트 (개발팀 → QA 핸드오프)

- [ ] `npm run build` 성공
- [ ] `npm run lint` (eslint + tailwind plugin)
- [ ] `npm run test` (Vitest, 단위) — 스토어·diff·wordCount
- [ ] `npm run e2e` (Playwright) — S1~S7 + 3개 인터랙션 플로우
- [ ] `axe-core` 0 violation
- [ ] 다크/라이트 토큰 시각 회귀 (Chromatic 또는 백분위 비교)
- [ ] NFR-101(입력 지연) — CodeMirror 6 가상 스크롤 실측 < 100ms
- [ ] NFR-103(로어북 검색) — 100건 데이터셋에서 < 1s
- [ ] NFR-203(백업) — DB 파일 export → import 왕복 무결성
- [ ] **AI 결과 자동 삽입 회귀 테스트** — S5 호출 → 결과 도착 → 사용자가 "끼워넣기" 누르기 전까지 본문/필드 무변경 (E2E)

---

## 8. 산출물 매핑 (디자인팀 → 개발팀 핸드오프)

| 본 문서 섹션 | 개발팀 1차 구현 |
|------------|----------------|
| §1 레이아웃 (3분할) | `App.tsx` + `TopBar` + `LeftSidebar` + `RightPanelSheet` + `StatusBar` |
| §2.1 S1 홈 | `pages/Home.tsx` + `features/ProjectCardGrid.tsx` |
| §2.2 S2 에디터 | `pages/Editor.tsx` + `components/editor/Editor.tsx` (CM6, v1.1: 에디터 메뉴·내보내기·FR 정정) |
| §2.3 S3 갤러리 | `pages/CharacterGallery.tsx` + `features/CharacterCard.tsx` |
| §2.4 S4 로어북 | `pages/Lorebook.tsx` + `features/LoreEntryCard.tsx` |
| §2.5 S5 AI 패널 | `components/panels/AiPanel.tsx` + `lib/aiStream.ts` (v1.1: NFR-201 전송 고지 Alert 추가) |
| §2.6 S6 윤문 리포트 | `components/panels/RefineReport.tsx` + `components/panels/DiffView.tsx` (v1.1: 50% 게이트 시 강제 수락 경로 삭제, taxonomy ID 매핑) |
| §2.7 S7 설정 | `pages/Settings.tsx` (5개 탭, v1.1: api_key [표시] 삭제 + NFR-404 Alert + 줄 번호 토글) |
| §3 인터랙션 플로우 | §3.1 → `aiPanelStore.insertOrReplace`, §3.2 → `useRefine`, §3.3 → `useCharacterCRUD` |
| §4 Tailwind 토큰 | `styles/globals.css` (CSS 변수) + `tailwind.config.ts` |
| §5 Zustand 스토어 | `stores/{editor,aiPanel,settings,ui}Store.ts` |
| §6 단축키·a11y | `App.tsx` keymap + Radix + axe-core |
| §7 구현 노트 | 본 섹션이 1차 스프린트 백로그 |

---

## 9. 다음 단계

- ✅ 본 문서(`디자인_화면설계서_v1.md` → v1.3 갱신) — 사양 v0.4 F-xxx·기술설계 A-xxx 정합, S-### ID 체계(§0-A), 결정사항_G4 Q1/Q3/Q4 반영, 시니어 배려 모드(§6.3) 완료
- → **QA팀 재검토 요청**: §2.6(50% 강제 수락 경로 삭제)·§2.7(api_key [표시] 삭제)·§4.1(cat-A~J 재매핑) 중심 증분 검증
- → **기획팀 확인 요청**: v0.3 사양과 v1.1 디자인의 정합성 — 사양 §3 S2 내보내기 / §3 S5 NFR-201 / §3 S7 NFR-404 / §5 taxonomy ID / §8.3 50% 차단 모두 일치
- → **개발팀 (5단계, ox-alpha-free)**: §7 우선순위대로 구현, §1.3 컴포넌트 트리 + §5 스토어 경계 + §4 토큰을 토대로 스캐폴딩
  - v1.1 신규/변경 구현 항목: ① S2 에디터 메뉴(내보내기 .txt/.md) ② S5 NFR-201 Alert ③ S7 api_key 변경 Dialog ④ S7 규정·현황 탭 NFR-404 Alert ⑤ S6 50% 게이트 block UI(재실행/폐기) ⑥ cat-A~J 토큰 + taxonomy ID 매핑 헬퍼 ⑦ Compartment lineNumbers 토글 ⑧ S1 카드 상태 집계 API/표시 로직
- → 디자인 추가 변경은 본 문서 v1.2로 누적(상단 CHANGELOG 갱신)

---

*본 문서는 사양 **v0.4** §6 UI 가이드라인·§9 F-xxx·§11 Q1~Q5 결정안과 기술설계 v1의 A-### API를 1:1 반영하며, AGENTS.md §1의 "프론트엔드 디자인 = MiniMax M3" 라우팅 규칙을 따른 산출물입니다. v1.1은 QA_기획정합성_리포트.md 처리 결과이며, v1.3은 G5 디자인 게이트 산출물입니다.*
