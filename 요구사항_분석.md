# 요구사항 분석·현황 확인 보고서

> **상태: historical 현황 보고 (2026-09-06).** 이 문서는 당시 발견사항과 수정
> 근거를 보존한다. 현재 구현·provider·검증 상태는 `HANDOFF.md` 및
> `docs/DOCUMENT_STATUS.md`가 우선하며, 이 문서의 LM Studio/endpoint/API key 관련
> 미해결 항목은 고정 ChatGPT OAuth 전환으로 superseded 되었다.

> 확인일: 2026-09-06
> 대상: 현재 워킹트리 전체(문서, FastAPI 백엔드, React 프론트엔드, 테스트)
> 목적: 웹소설 자동화 대시보드로서 현재 구현 범위와 요구사항 충족 여부 확인
> 방법: 저장소 구조·문서·코드 정적 점검, 프론트 빌드, 백엔드 테스트 실행 시도

## 1. 결론

이 프로젝트는 **웹소설 작가의 기획·집필·AI 보조·윤문·연재 준비를 한곳에서 처리하는 로컬 우선 대시보드 MVP**로 구성되어 있다.

핵심 골격은 구현되어 있다.

- 백엔드: FastAPI + SQLAlchemy + SQLite(WAL) + Alembic
- 프론트엔드: Vite + React + TypeScript + CodeMirror + TanStack Query + Zustand
- 도메인: 프로젝트·회차·권, 캐릭터·관계, 로어북·FTS 검색, AI 엔드포인트·프리셋·SSE 스트리밍, 윤문·diff·변경률 게이트
- 보안: api_key 암호화 저장 및 API 응답 마스킹, 로컬 데이터 우선 정책
- 추가 기능: 장르/프리미스 기반 작품 부트스트랩, 노벨피아 PLUS 현황 위젯, 오픈소스 라이선스 고지

다만 현재 상태를 **“모든 요구사항이 구현된 출시 완료”라고 보기는 어렵다.** 문서상 G8 통과 기록과 실제 코드·추적성 자료 사이에 차이가 있고, 아래 항목은 실제 사용 전에 보완 또는 명시적인 범위 조정이 필요하다.

## 2. 확인된 구현 범위

### 구현된 주요 흐름

1. 홈에서 프로젝트 생성·열기·삭제
2. 프로젝트 안에서 회차 생성·삭제·상태/메모 수정·본문 편집
3. Markdown 미리보기와 공백 포함/제외/노벨피아식 카운트 표시
4. 캐릭터 카드 CRUD, 관계 라벨 관리, 로어북 CRUD·카테고리·키워드·검색
5. OpenAI 호환 엔드포인트 등록과 모델 목록 조회
6. 프롬프트 프리셋 기반 AI SSE 스트리밍 및 결과 수동 삽입/선택 교체/복사
7. im-not-ai 기반 윤문 실행, diff, 30% 경고·50% 차단, 수락/거절
8. 규정 안내, PLUS 상태 계산, 라이선스/출처 안내

근거: `backend/app/main.py`, `backend/app/routers/`, `frontend/src/pages/`, `frontend/src/components/panels/`, `frontend/src/components/home/PlusStatusWidget.tsx`.

## 3. 우선순위가 높은 문제·불일치

### P0 — 백업/복원 기능이 요구사항과 다르게 미구현

- 요구사항 `R-043 (NFR-203)`은 데이터 전체의 파일 단위 백업·복원을 요구한다(`요구사항_정의서.md:168`).
- 사양의 `F-034`도 MVP 기능으로 기록되어 있다(`대시보드_MVP_사양.md:378`).
- 하지만 설정 화면은 “작품 데이터 백업·복원 기능은 추후 제공 예정”이라고 표시한다(`frontend/src/pages/SettingsPage.tsx:626`).
- 추적성에서도 `R-043`은 `planned` 상태이며, 기술설계는 REST 엔드포인트를 만들지 않는 것으로 정리되어 있다(`traceability.json:675-683`, `기술설계_v1.md:204`).

판정: **Must 요구사항 미충족**. MVP에서 제외할 것이라면 요구사항·사양·게이트 기록을 함께 수정해야 하며, 유지할 것이라면 최소한 앱 종료 상태의 SQLite 백업/복원 UX를 구현해야 한다.

### P0 — 페이지 이탈 시 자동저장 플러시가 실제 API 계약과 맞지 않음

- 프론트는 이탈 시 `navigator.sendBeacon()`으로 `/api/v1/chapters/{id}/content`에 요청한다(`frontend/src/pages/EditorPage.tsx:314-325`).
- `sendBeacon`은 POST 요청을 보내지만 백엔드는 같은 경로에 `PUT`만 등록되어 있다(`backend/app/routers/projects.py:186-194`).

판정: 대기 중인 변경분이 페이지 이탈 때 보존된다는 보장이 없다. NFR-204 및 `R-044` 검증 전에 `fetch(..., { keepalive: true, method: 'PUT' })` 같은 실제 PUT 경로 또는 별도 POST 수신 경로를 정해야 한다.

### P1 — LM Studio 자동등록 payload가 기본 모델 필드명을 잘못 사용

- 실행 스크립트는 자동 등록 시 `model: "auto"`를 보낸다(`scripts/dev.sh:41-44`).
- 백엔드 스키마와 AI 호출 로직이 인식하는 필드는 `default_model`이다(`backend/app/schemas.py:209-248`, `backend/app/routers/ai_panel.py:68-75`).

판정: 자동 등록된 LM Studio 엔드포인트는 기본 모델이 비어 있어 첫 AI 호출에서 “모델이 지정되지 않았습니다”가 될 가능성이 높다. `default_model`로 통일하고 자동등록 후 실제 모델 조회/선택까지 확인해야 한다.

### P1 — 노벨피아 카운트와 서버 PLUS 판정 기준이 서로 다름

- 프론트 푸터는 문자·숫자만 세는 `novelpia` 값을 사용해 3,000자 충족 여부를 표시한다(`frontend/src/lib/wordCount.ts:9-22`, `frontend/src/components/editor/WordCountFooter.tsx:9-26`).
- 반면 백엔드는 본문 저장 시 공백만 제거한 `word_count_cache`를 저장한다(`backend/app/services/wordcount.py:11-15`, `backend/app/routers/projects.py:186-194`).
- PLUS 상태 API는 이 `word_count_cache`로 판정한다(`backend/app/routers/projects.py:238-260`).

판정: 같은 원고에서 에디터는 미달, PLUS 위젯은 충족으로 보일 수 있다. 플랫폼 실측 기준을 하나로 확정하고 프론트 표시·서버 캐시·테스트·문서를 동일 기준으로 맞춰야 한다. 현재 부록06도 공식 기준 미확인 상태이므로 “정확히 일치”로 표기하기에는 근거가 약하다.

### P1 — nullable volume 전환이 TypeScript/UI에 완전히 반영되지 않음

- 백엔드 `Chapter.volume`은 nullable이다(`backend/app/schemas.py:41-69`).
- 프론트 타입은 `volume: number`로 남아 있다(`frontend/src/lib/api.ts:73-84`).
- 에디터·사이드바·내보내기에서 `volume`을 바로 산술 처리하거나 `{volume}권`으로 출력한다(`frontend/src/pages/EditorPage.tsx:50-52,211`, `frontend/src/components/layout/LeftSidebar.tsx:119-141`, `frontend/src/lib/export.ts:49-53`).

판정: 권 없는 평면 회차에서 `null권` 표시, 정렬 오류, 내보내기 제목 오류가 발생할 수 있다. 타입을 `number | null`로 바꾸고 정렬/표시/내보내기 폴백을 공통 함수로 정리해야 한다.

### P2 — AI 컨텍스트 체크박스가 캐릭터·로어북에서는 비활성

- AI 패널의 현재 회차 체크박스는 토글 가능하지만, 선택 캐릭터와 선택 로어북 체크박스는 `disabled` 상태다(`frontend/src/components/panels/AiPanel.tsx:308-327`).
- 캐릭터/로어북 상세 화면의 진입 버튼을 통해서만 선택값이 주입된다(`frontend/src/pages/CharactersPage.tsx:293-301`, `frontend/src/pages/LorebookPage.tsx:275-279`).
- 요구사항 `R-023 (FR-404)`은 실행 시 세 컨텍스트를 체크박스로 포함/제외할 수 있어야 한다(`요구사항_정의서.md:121`).

판정: 진입점 기반 UX는 동작하지만, 패널에서 사용자가 포함 여부를 직접 끄거나 켤 수 없다. Should 요구사항을 충족하려면 비활성 표시 대신 선택 상태 토글을 제공해야 한다.

### P2 — 설정 화면의 reasoning_effort UI가 빠져 있음

백엔드에는 `reasoning_effort`가 저장·전달되지만(`backend/app/schemas.py:209-248`, `backend/app/routers/ai_panel.py:249-258`), 프론트 타입·설정 폼에는 입력 UI가 없다(`frontend/src/lib/api.ts:173-200`). 기술설계 문서도 이 점을 미구현 한계로 기록한다(`기술설계_GPT_OAuth_브릿지_v1.md:121`). API 직접 수정 없이는 사용자가 모델별 추론 강도를 바꿀 수 없다.

## 4. 문서·게이트 상태의 신뢰성

- `HANDOFF.md`는 2026-09-06 기준 `pytest 119 passed`와 E2E `6/6 passed`를 기록한다(`HANDOFF.md:229-242`).
- `QA_개발검증_리포트_v2.md`는 2026-08-26 문서로, 이미 구현된 라이선스 고지를 여전히 “릴리스 차단·미구현”으로 적고 있다(`QA_개발검증_리포트_v2.md:9-14`).
- `gates.json`은 G0~G8 전체 pass로 기록되어 있으나, 추적성의 일부 요구사항은 여전히 `planned` 상태다(`gates.json:1-80`, `traceability.json:201-206,675-683,814-820`).

판정: 문서가 서로 다른 시점의 상태를 가리킨다. **최신 코드·최신 QA 결과·traceability·gates의 기준일을 하나로 맞춘 뒤 출시 판정을 다시 내리는 작업이 필요하다.**

## 5. 이번 실행 검증 결과

### 통과

- `frontend`: `npm run build` 성공
  - TypeScript 컴파일 및 Vite 번들 생성 성공
  - 223 modules transformed
  - 최종 JS 약 908KB로 chunk size 경고가 있으나 빌드 실패는 아님
- `git diff --check`: 공백/패치 형식 오류 없음
- 앱 import 및 lifespan 수동 실행: SQLite 초기화·FTS 인덱스·기본 프리셋 시드 단계는 정상 완료

### 확인 불가/환경 블록

- `backend`: pytest가 119개를 수집한 뒤 첫 `TestClient` fixture 설정에서 무응답 상태가 됐다.
- `/tmp` SQLite를 지정한 단일 테스트도 60초 timeout으로 종료됐다.
- 비교용 단순 FastAPI 앱의 `TestClient`도 같은 환경에서 진입이 멈췄으므로, 이 현상은 현재 Python 3.14/Starlette·AnyIO 실행환경 문제일 가능성이 높으며 애플리케이션 결함으로 확정하지 않는다.
- 따라서 이번 점검에서는 “백엔드 119 passed”를 새로 재현했다고 판정하지 않는다.

## 6. 권장 처리 순서

1. 백업/복원 범위를 MVP에 포함할지 결정하고 문서·추적성·UI를 일치시킨다.
2. 자동저장 종료 플러시를 실제 PUT 계약으로 수정한 뒤 강제 종료 복구 테스트를 다시 실행한다.
3. `default_model`, nullable `volume`, 노벨피아 글자 수 기준을 프론트·백엔드·스크립트·테스트에서 통일한다.
4. Playwright/E2E와 백엔드 테스트 실행 환경을 ext4 기반 WSL 또는 호환되는 Python/Starlette 조합으로 고정한다.
5. 최신 코드 기준 QA 리포트·traceability·gates·HANDOFF를 한 번에 갱신한 뒤 출시 판정을 재검토한다.

## 7. 작업 범위 메모

이번 확인에서는 기존 코드와 문서를 수정하지 않았고, 본 보고서만 새로 추가했다. 워킹트리에 이미 존재하던 수정·신규 파일은 사용자 변경사항으로 보고 보존했다.
