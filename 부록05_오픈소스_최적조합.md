# 부록 05 — 오픈소스 최적 조합 분석

> **작성일**: 2026-08-25 / **조사 방법**: GitHub(ungh 프록시)·npm·PyPI 레지스트리 실측 + 직접 HTTP 검증
> **근거 문서**: `요구사항_정의서.md`(승인 완료), `대시보드_MVP_사양.md` v0.2
> **개정 (2026-08-25)**: `QA_기획정합성_리포트_v2.md` R-3 정정 — §② 매트릭스에 신규 의존성 JSZip(MIT 선택, npm 실측 3.10.1) 등재
> **개정 (2026-08-25)**: `QA_기획정합성_리포트.md` m-5 정정 — §② 매트릭스 UI 라이브러리·상태관리 행의 "확정 예정"을 사양 v0.2 확정값(shadcn/ui+Tailwind·Zustand)으로 수정, 근거 문서 표기도 v0.2로 갱신
> **비고**: Serper 웹검색 키 미설정으로 일반 웹 검색은 미실시. 모든 수치는 레지스트리 API 실측값. 미확인 항목은 명시함.

---

## ① 레이어별 비교표

### L1. 프론트엔드 프레임워크

| 후보 | 라이선스 | ⭐(GitHub) | 최근 push | npm 최신 | 평가 |
|------|---------|-----|-----------|----------|------|
| **Vue 3** ✅ | MIT | 54,243 | 2026-08-25 | 3.5.41 (2026-08-05) | 경량 SPA, 한국어 자료 풍부, minimax 디자인 세션과 표준 컴포넌트 구조로 협업 가능 |
| Svelte 5 | MIT | 87,984 | 2026-08-24 | 5.56.10 (2026-08-20) | 번들 최소·성능 우수하나 컴포넌트 라이브러리·한국어 생태계가 Vue 대비 얇음 |
| React | MIT | 247,857 | 2026-08-24 | (미측정) | 생태계 최대이나 단일 사용자 로컬 앱에는 과중, 팀 라우팅(minimax) 부합도 낮음 (→ 사용자 최종 결정: 채택) |

### L2. 소설 원고 에디터 (수만 자 타이핑 성능 최우선)

| 후보 | 라이선스 | ⭐ | npm 최신 / 배포일 | 평가 |
|------|---------|-----|-------------------|------|
| **CodeMirror 6** ✅ | MIT | 7,821(dev) | @codemirror/view 6.43.9 (**2026-08-16**) | 가상 스크롤 렌더링으로 수만~십만 자 문서에서도 입력 지연 없음(설계 목표). @codemirror/lang-markdown 지원 |
| Monaco | MIT | 46,582 | 0.56.0 (2026-07-20) | VS Code 편집기. 코드 편집 특화, 번들 수 MB급, 산문·한국어 IME 집필 워크플로 부적합 |
| TipTap 3 | MIT | 38,155 | @tiptap/core 3.30.3 (2026-08-24) | ProseMirror 기반 WYSIWYG. 활발하나 장문을 DOM에 유지하는 구조라 수만 자 타이핑 리스크 |
| Milkdown | MIT | 11,850 | (npm dist-tags 확인 불가 — 미확인) | ProseMirror+remark 조합, 플러그인 많음. 커뮤니티 작고 장문 성능 실증 부족 |

### L3. 백엔드 + ORM

| 후보 | 라이선스 | ⭐ | PyPI 최신 / 업로드 | 평가 |
|------|---------|-----|--------------------|------|
| **FastAPI + SQLAlchemy 2.x** ✅ | MIT / MIT | 101,822 / 12,104 | 0.141.1 (2026-07-29) / 2.0.52 (2026-08-11) | SSE 스트리밍 용이(sse-starlette 3.4.8 BSD-3 병행), Pydantic 검증, 생태계 최대 |
| Litestar | MIT | 8,408 | 2.24.0 (2026-06-11) | 성능·DI 우수하나 생태계·한국어 자료가 FastAPI 대비 크게 적음(전환 이득 없음) |
| FastAPI + SQLModel | MIT | 18,281 | 0.0.39 (2026-06-25) | 여전히 0.0.x 베타. 원고 데이터 무결성이 중요한 본 프로젝트엔 안정성 리스크 |

### L4. DB (SQLite 성능 튜닝)

**대안 불필요 판단.** 단일 사용자 + 파일 1개 백업(NFR-201/203)에서 SQLite가 정답. 접속 시 표준 프라그마:

```
PRAGMA journal_mode=WAL;      -- 읽기·쓰기 병행(자동저장+조회)
PRAGMA synchronous=NORMAL;    -- WAL 모드에서 안전한 성능 최적점
PRAGMA busy_timeout=5000;     -- 쓰기 잠금 대기
PRAGMA foreign_keys=ON;
```

- WAL은 앱 비정상 종료 시 마지막 커밋까지 보존(NFR-204)에 부합.
- 로어북 검색(FR-304)은 FTS5 인덱스를 선택 적용.

### L5. SillyTavern 캐릭터 카드 V2/V3 파서

| 후보 | 라이선스 | 상태 | 비고 |
|------|---------|------|------|
| **자체 파서(권장)** ✅ | — | — | 카드 V2/V3는 PNG tEXt chunk('chara') + base64 JSON 구조로 단순. 사양 공개(character-card-spec-v2 184⭐ / kwaroran/character-card-spec-v3 107⭐ — 사양문서 저장소). Python 생태계 성숙 파서 **미확인**(PyPI: character-card-parser, chara-card, pytavern 등 전부 404 확인) |
| @motioneffector/cards (npm) | MIT | 1.0.1 (2026-02-12), ⭐2 | V3 카드+로어북 파서. 라이선스 양호하나 도입 사례 극소 → 참고용 |
| sillytavern-utils-lib (npm) | MIT | 1.0.68 (2026-06-09) | ST 유틸 모음, 활동적. 카드 전용 파서라기보다 보조 유틸 |
| @lenml/char-card-reader (npm) | **AGPL-3.0** | 1.1.1 | 라이선스 오염 위험 → 배제 |
| SillyTavern 자체 코드 | **AGPL-3.0** (32,612⭐, push 2026-08-21) | 활발 | 코드 복사 금지. 포맷·동작 참고만 |

### L6. 로어북 참고 구현

| 참고 대상 | 라이선스 | 활용 방식 |
|-----------|---------|-----------|
| SillyTavern World Info | AGPL-3.0 | "키워드 매칭 → 컨텍스트 자동 주입" 개념 참고 전용(코드 복사 금지). keywords[]-content 구조는 사양 v0.1 §4.2와 이미 정합 |
| novelWriter (GPL-3.0, ⭐3,088, 2026-08 활동 — 부록01) | GPL-3.0 | 챕터/장면 계층·태그 참조 모델 참고. `.nwx` 임포트는 백로그(P2) — 코드 비복사 조건 하에 연계 가능 |
| RisuAI lorebook (GPL-3.0) | GPL-3.0 | 포맷 참고만 |

### L7. OpenAI 호환 클라이언트 (SSE 스트리밍 관점)

| 후보 | 라이선스 | 최신 / 배포 | 평가 |
|------|---------|-------------|------|
| **openai SDK (Python)** ✅ | Apache-2.0 | 3.3.1 (2026-08-19) | `base_url` 오버라이드로 LM Studio·KoboldCpp·클라우드 즉시 호환(FR-401). SSE 스트리밍 내장(httpx 기반, FR-405 최단 경로) |
| httpx 직접 호출 | BSD-3-Clause | 0.28.1 (2024-12-06, 안정세션) | 의존성 최소. 단 SSE 파싱·재시도·타임아웃을 직접 구현 필요 → 폴백용 |
| LiteLLM | MIT | 1.98.0 (2026-08-22) | 멀티프로바이더 추상화는 강력하나 의존성 트리 무거움(기확인 리스크). OpenAI 호환 단일 경로만 필요한 본 프로젝트엔 과잉 → 배제 |

### L8. diff 뷰 + im-not-ai 연동 패턴

| 후보 | 라이선스 | 최신 / 배포 | 평가 |
|------|---------|-------------|------|
| **jsdiff (`diff`)** ✅ | BSD-3-Clause | 9.0.0 (2026-04-13), ⭐9,194 | 문자/단어 단위 diff → 진단 span 하이라이트(S6)와 변경률 게이트 계산(FR-505)에 적합. 한국어 조사·어미 변화 대응은 문자 단위 diff 사용 |
| @codemirror/merge | MIT | 6.12.2 (2026-06-09) | 에디터 내 병렬 merge 뷰 옵션. CodeMirror 채택 시 자연 연동 |
| diff-match-patch | Apache-2.0 | 1.0.5 (**2020-05-20**) | 마지막 배포 2020년 — 유지보수 정체 → 배제 |

연동 패턴: 백엔드가 im-not-ai 서브프로세스 실행 → 원문/수정본+카테고리 span 반환 → 프론트가 jsdiff로 재계산·하이라이트 → 수락 시 `POST /api/v1/refine/runs/{id}/accept`. 자동 덮어쓰기 없음(FR-504) 유지.

### L9. UI 컴포넌트 라이브러리 + 상태관리

| 후보 | 라이선스 | 최신 / 배포 | 평가 |
|------|---------|-------------|------|
| **Naive UI + Pinia** ✅ | MIT | 2.45.2 (2026-08-21) / pinia 4.0.3 (2026-08-12) | TypeScript 네이티브, 다크 테마 내장, 테마 커스텀 용이(저채도 세리프 톤 요구 부합), S2~S7 필수 컴포넌트(트리·드로어·탭) 충족 |
| PrimeVue | npm 메타데이터 "SEE LICENSE IN LICENSE.md"(⚠️) | 5.0.1 (2026-08-13) | 저장소 LICENSE.md는 2026-08-25 기준 MIT로 직접 확인했으나, npm license 필드 불명확 + 과거 라이선스 변경 논란 이력 → 배제(§④-2) |
| Vuetify 4 | MIT | 4.1.11 (2026-08-19) | Material 디자인 강제 → "SaaS 느낌 배제" UI 방침과 충돌 |
| shadcn-vue | 미확인 | 실측 미실시 | Radix-Vue 기반 복사 방식. 다크 테마 우수하나 검증 부족 → 대안 후보로만 |

---

> **⚠️ 최종 결정 갱신 (2026-08-25)**: 사용자가 프론트 프레임워크를 **React**로 최종 확정함.
> 아래 매트릭스의 "프론트 프레임워크 1순위 = Vue 3 + Vite" 항목은 본 결정으로 **대체됨** (L1 비교표의 실측 데이터는 참고 자료로 유지).
> 에디터 등 나머지 레이어 선택은 CodeMirror 6·jsdiff 등 React와 무관하게 유효.

## ② 최종 추천 조합 매트릭스

| 레이어 | 1순위 | 대안 (전환 조건) |
|--------|-------|------------------|
| 프론트 프레임워크 | **Vue 3 + Vite** | Svelte 5 — 컴포넌트 라이브러리 제약 발생 시 |
| 에디터 | **CodeMirror 6** (+lang-markdown) | TipTap 3 — WYSIWYG 요구 확대 시(장문 성능 재검증 선행) |
| 백엔드 | **FastAPI** | Litestar — 전환 근거 없음(유지) |
| ORM | **SQLAlchemy 2.x** (+Alembic) | SQLModel — 1.0 안정화 확인 시 재평가 |
| DB | **SQLite + WAL 튜닝** | 대안 없음(파일 1개 백업 필수) |
| ST 카드 파서 | **자체 구현**(V2/V3 사양 준수) | @motioneffector/cards(MIT) 코드 참고 |
| 로어북 설계 참고 | ST World Info 개념 참고 | novelWriter 태그 모델 |
| AI 클라이언트 | **openai SDK(base_url 오버라이드)** | httpx 직접 스트리밍 — SDK 비호환 엔드포인트 발견 시 |
| diff | **jsdiff** (선택: @codemirror/merge) | diff-match-patch — 배제(정체) |
| zip(내보내기) | **JSZip** — FR-109 프로젝트 내보내기(전 회차 .zip 묶음, 디자인 v1.1 §2.2 S2). npm 실측 3.10.1(2022-08-02), 라이선스 `(MIT OR GPL-3.0-or-later)` → **MIT 선택** 적용 | 개별 .txt 다운로드 반복 — zip 불필요 시 제거 가능 |
| UI 라이브러리 | **Naive UI** (Vue 전용 — React 채택으로 무효, React용 확정: **shadcn/ui+Tailwind — 사양 v0.2 확정**) | shadcn-vue — 검증 후 |
| 상태관리 | **Pinia** (Vue 전용 — React 채택으로 무효, React용 확정: **Zustand — 사양 v0.2 확정**) | — (Vue 공식, 유지) |

---

## ③ 선정 근거 요약

1. **에디터가 병목**: NFR-101(3~5만 자 무지연)이 가장 까다로운 제약. CodeMirror 6만이 가상 렌더링으로 이를 구조적으로 만족하며, npm 2026-08-16 배포로 활성 확인.
2. **모델 무관은 openai SDK 한 방**: `base_url` 교체만으로 LM Studio·KoboldCpp·클라우드 전환(NFR-503). SSE 스트리밍(FR-405) 내장.
3. **원고 주권 = 파일 1개**: SQLite+WAL은 자동저장·크래시 복구(NFR-204)와 백업(NFR-203)을 동시에 만족. 추가 DB 불필요.
4. **라이선스 방어선**: 직접 도입은 MIT/Apache/BSD만. AGPL(SillyTavern·KoboldCpp)과 GPL(novelWriter)은 코드 비복사 원칙 하에 참고만.
5. **경량 유지**: LiteLLM·Monaco 등 무거운 선택지를 배제해 비전공자 Windows PC(NFR-502)에서도 가볍게 실행.

---

## ④ 리스크·주의점

1. **CodeMirror 6 GitHub 아카이브**: codemirror 조직 저장소 전반이 **2026-04-15~16 아카이브(read-only)로 실측 확인**(GitHub HTML: "This repository was archived by the owner on Apr 15, 2026"). 다만 npm 패키지는 그 이후인 @codemirror/view 6.43.9(2026-08-16)까지 배포 중이고, 저장소 필드가 저자 자체 Git 서버 `code.haverbeke.berlin`으로 변경되어 있음 → 개발 인프라 이전으로 판단. 단, GitHub 외부 서버 의존은 공급망 가시성 저하 요소이므로 분기별 npm 배포 여부 모니터링 필요.
2. **PrimeVue 라이선스 불명확**: npm license 필드가 "SEE LICENSE IN LICENSE.md"로 표기되고 과거 라이선스 변경 논란 이력 존재. 저장소 LICENSE.md는 2026-08-25 현재 MIT이나, 라이선스 정책 변동 가능성 자체가 도입 리스크 → Naive UI 대신 채택 근거 없음.
3. **LiteLLM 무게**: 의존성 트리가 커서 설치·실행 부담. OpenAI 호환 단일 경로만 필요한 본 앱엔 과잉 → openai SDK 채택으로 회피.
4. **ST 카드 파서 생태계 부재(Python)**: PyPI에 성숙 파서 미확인. npm MIT 파서(@motioneffector/cards)는 ⭐2 수준으로 공급망 리스크 → 자체 구현이 오히려 안전. 포맷이 단순해 구현 비용 낮음.
5. **SQLModel 베타**: 0.0.x 지속. 원고 데이터 무결성 요구상 SQLAlchemy 2.x 직용 권장.
6. **AGPL/GPL 오염 금지**: SillyTavern·KoboldCpp(AGPL), novelWriter(GPL) 코드 복사 금지. 포맷·UX 참고만 허용. 향후 PNG 카드 임포트 기능도 자체 파서로 구현.
7. **httpx 릴리스 주기**: PyPI 최종 배포 2024-12로 느리나 openai SDK 내부 의존으로 안정적으로 관리됨. 폴백 사용 시에만 직접 버전 고정 권장.

---

## ⑤ 사양 문서 v0.1 변경 필요 목록

| # | 위치 | 변경 내용 | 사유 |
|---|------|-----------|------|
| 1 | §2.1 마크다운 에디터 행 | "marked/markdown-it" 중 **markdown-it(+DOMPurify)로 확정** 표기 | marked도 MIT·활동적(18.0.11)이나 markdown-it 15.0.0(2026-07-30)이 플러그인 구조 유리. 미리보기 XSS 방지용 DOMPurify 명시 필요 |
| 2 | §2.1 기술 스택 표 | **UI 컴포넌트 라이브러리 행 추가: Naive UI(MIT)** + 다크 테마 내장 활용 명시 | v0.1에는 UI 라이브러리 결정 누락 |
| 3 | §2.1 AI 통신 행 | "openai SDK 또는 httpx" → **openai SDK 채택, httpx는 폴백**으로 결정 표기 | 실측 기준 최종 결정 반영 |
| 4 | §2.1 / 신설 | SQLite 접속 프라그마 표준(WAL·synchronous=NORMAL·busy_timeout·foreign_keys)을 기술 스택 또는 NFR 구현 노트에 명시 | NFR-204 대비 및 성능 보장 |
| 5 | §5 윤문(M5) API | `/api/v1/refine` 응답 스키마에 **span 목록(카테고리·오프셋)** 포함 명시 + 프론트 diff 계산은 jsdiff(BSD-3) 사용 명시 | S6 화면 구현 규격화 |
| 6 | §7 백로그 P1 | ST 카드 임포트/익스포트 항목에 "**자체 V2/V3 파서 구현**(@motioneffector/cards 등 MIT 참고, AGPL 코드 비복사)" 주석 추가 | Python 파서 생태계 부재 실측 반영 |
| 7 | §8 리스크 | CodeMirror 개발 저장소 이전(code.haverbeke.berlin)·PrimeVue 라이선스·LiteLLM 배제 사유를 리스크 목록에 추가 | 본 부록 §④ 반영 |
| 8 | §6 UI 가이드라인 | "기술: Vue 3 + Vite, Pinia, CodeMirror 6"에 **Naive UI, jsdiff** 추가 | minimax 세션 전달값 완결 |

---

*작성: 리서치팀 (2단계) — 실측 데이터 기반. 다음 단계: 사양 문서 v0.2 갱신(위 ⑤ 반영) 후 디자인(minimax) 세션으로 핸드오프.*
