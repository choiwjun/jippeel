# AI-context contract rulings — parent draft review

Status: binding corrections to the draft; planner applies them to spec and plan before implementation.

1. Reuse preservation 409 detail.code="revision_conflict" (not chapter_revision_conflict), current_revision field and stable existing helper; reduces incompatible UI errors.
2. Remove public RelationshipScope/relationship_scope options. They are unnecessary new surface and canon selected mode has no character_ids. Target policy only: gen opt-in includes relationships whose BOTH endpoints in validated selected characters; canon opt-in all same-project relationships. 0/1 selected characters means empty relationship list (metadata0), NOT422; checkbox need not create an unrelated error. Validate any consumed relationship endpoint ownership, no fabricated names.
3. CanonDialog MUST flush same chapter through existing manuscript coordinator, abort on recovery/conflict/network/navigation mismatch, then capture actual saved revision; cached-query revision alone in §9.5 contradicts the snapshot goal. Canon backend must capture immutable input body+revision+SHA256 before awaiting provider and log checked_input_revision+checked_input_hash or equivalent explicit names in context_json and checked_context response. Log remains bound to origin even if chapter changes while provider runs. Add real regression.
4. Request start safety: reserve an in-flight start token BEFORE awaiting flush; prevent double-click duplicate provider starts, and invalidate pending starts on close/cancel/navigation. Snapshot settings and origin consistently, do not start a stale request after flush finishes. Returned AI/canon result stays labelled with captured origin; no current-editor mutation on result arrival. Manual insertion still requires existing recovery/edit lock in addition to project/chapter equality. Copy remains enabled.
5. Purpose exceptions must apply to parallel planner instructions, not only parse schema. Give parse_parallel_plan(raw, episode_purpose="serial") explicit stable interface, old helper callers default serial. Do not claim all legacy hook-only planner JSON valid for finale (needs last-scene ending_intent).
6. Narrow future-reference rule: future PLANTED chapter means not-yet-fact; future RESOLVED chapter does not make an already planted clue nonexistent. Retain current planted clue, label its resolution as future plan; do not use future planned resolution as evidence of violation. Explicit approval always included even with auto_foreshadow=false/limit exhausted; permission scoped only listed IDs. Approved future plan may be realized by author permission, not asserted as preexisting fact. Validate project references for ALL consumed foreshadow rows, not just approved list.
7. Acceptance criteria source-frozen applies to DRAFTING worker only, not final implementation. Current record style/lore not historical truth; historical memory deferred. Fix body inclusion/canon stage helper API signatures consistently; parent will arrange independent preflight after your report.
Baseline probes now passed native intercepted-message reproductions, see docs/audits/ai-context-baseline-probes.md/.json and use actual negative corpus in test plan. Keep plan bounded; no new broad refactors. Reply when both docs ready.